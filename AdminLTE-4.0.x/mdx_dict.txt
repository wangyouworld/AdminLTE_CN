## What is AdminLTE?
## 什么是 AdminLTE？
AdminLTE is a free, MIT-licensed admin dashboard template built on **Bootstrap 5.3.8** with vanilla TypeScript (no jQuery). It ships responsive layouts, a sidebar navigation system, light/dark/auto color mode, RTL support, and a growing catalog of demo pages — calendar, kanban, chat, file manager, invoice, profile, settings, and more.
AdminLTE 是一个免费的、基于 MIT 许可证的后台管理面板模板，基于 **Bootstrap 5.3.8** 构建，使用原生 TypeScript（无 jQuery）。它提供了响应式布局、侧边栏导航系统、亮色/暗色/自动颜色模式、RTL 支持，以及不断增加的示例页面——日历、看板、聊天、文件管理器、发票、个人资料、设置等。
It's a *template*, not a component library. You drop the markup into your own project (or framework templates) and customize the parts you need. Bootstrap 5 handles the components; AdminLTE provides the application shell.
它是一个*模板*，而不是组件库。将代码放在项目（或框架模板）中，并自定义所需部分。Bootstrap 5 负责组件；AdminLTE 提供应用外壳。
If you've never used AdminLTE before, start with [Getting Started](getting-started.html) — it walks through a working dashboard in ten minutes.
如果您从未使用过 AdminLTE，请从[入门指南](getting-started.html)入手——它会在十分钟内带您完成一个可用的仪表盘。
## Installation paths
## 安装方式
Pick the path that matches how you'll actually use AdminLTE:
有以下安装方式：
| Path | Best for | Setup time |
| 方式 | 适用场景 | 配置时间 |
| **CDN** | Prototyping, demos, simple sites | 1 minute |
| **CDN** | 原型构建、演示、简单站点 | 1 分钟 |
| **npm + bundler** | Production apps, framework projects (Laravel, Symfony, Rails) | 5 minutes |
| **npm + 打包工具** | 生产应用、框架项目（Laravel、Symfony、Rails） | 5 分钟 |
| **Build from source** | You need to customize SCSS variables, sidebar width, breakpoints | 10 minutes |
| **从源码构建** | 需要自定义 SCSS 变量、侧边栏宽度、breakpoint | 10 分钟 |
| **Composer** | PHP projects that prefer Packagist over npm | 2 minutes |
| **Composer** | 偏好 Packagist 而非 npm 的 PHP 项目 | 2 分钟 |
### CDN — fastest path
### CDN — 最快
Drop these four CSS tags into `<head>` and four script tags before `</body>`. No install, no build step:
将以下 4 个 CSS 标签放入 `<head>` 中，将 4 个脚本标签放在 `</body>` 之前。无需安装，无需构建步骤：
<!-- In <head> -->
<!-- 在 <head> 中 -->
<!-- Before </body> -->
<!-- 在 </body> 之前 -->
Then copy any [demo page](https://github.com/ColorlibHQ/AdminLTE/tree/master/dist) HTML into your own file. See [Getting Started](getting-started.html) for the minimum-viable layout.
然后将任意[演示页面](https://github.com/ColorlibHQ/AdminLTE/tree/master/dist)的 HTML 复制到您自己的文件中。有关最小可用布局，请参阅[入门指南](getting-started.html)。
### npm + bundler
### npm + 打包工具
Requires Node.js 18+ and a bundler (Vite, Webpack, Rollup, esbuild, etc.).
需要 Node.js 18+ 和打包工具（Vite、Webpack、Rollup、esbuild 等）。
Then import the CSS and JS in your app entry point:
然后在应用入口点中导入 CSS 和 JS：
Yarn and pnpm work the same way — substitute `yarn add` or `pnpm add` for the install.
Yarn 和 pnpm 的使用方式相同——将安装命令替换为 `yarn add` 或 `pnpm add`。
### Build from source
### 从源码构建
If you want to customize SCSS variables (sidebar width, breakpoints, brand colours), clone the repo and build locally:
如果您想自定义 SCSS 变量（侧边栏宽度、breakpoint、品牌颜色），请克隆仓库并在本地构建：
dev server with hot-reload at localhost:3000
开发服务器，支持热重载，地址为 localhost:3000
one-off production build (clean + lint + compile)
生产环境构建（执行清理、代码检查与编译）
The compiled output lands in `dist/`. Edit `src/scss/_variables.scss` or `src/scss/_bootstrap-variables.scss`, then rerun the build. See [Customization &amp; Theming](customization.html) for which variables matter.
编译后的输出文件位于 `dist/`。编辑 `src/scss/_variables.scss` 或 `src/scss/_bootstrap-variables.scss`，然后重新运行构建。有关变量调整的问题，请参阅[自定义与主题](customization.html)。
For PHP projects:
适用于 PHP 项目：
The package is published from the same source tree — you get the `dist/` folder ready to serve.
该包从相同的源码树发布——您可以直接使用 `dist/` 文件夹。
## Prerequisites
## 前置要求
| | Minimum | Recommended |
| | 最低要求 | 推荐版本 |
pinned in `package.json`
锁定在 `package.json` 中
used by CI
CI 使用
| Browser | Modern evergreen | Latest stable Chrome/Firefox/Safari/Edge |
| 浏览器 | 现代浏览器 | 最新稳定版 Chrome/Firefox/Safari/Edge |
AdminLTE 4 does **not** support Internet Explorer. If you need IE11, stay on [AdminLTE 3](https://github.com/ColorlibHQ/AdminLTE/releases/tag/v3.2.0) or earlier.
AdminLTE 4 **不**支持 Internet Explorer。如果您需要 IE11，请继续使用 [AdminLTE 3](https://github.com/ColorlibHQ/AdminLTE/releases/tag/v3.2.0) 或更早版本。
## Where to next
## 接下来去哪里
- **[Getting Started](getting-started.html)** — a working dashboard in 10 minutes
- **[入门指南](getting-started.html)** — 十分钟内完成一个可用仪表盘
- **[Layout](layout.html)** — the app-wrapper structure and responsive options
- **[布局](layout.html)** — 应用外壳结构和响应式选项
- **[Customization](customization.html)** — change colours, sidebar width, spacing
- **[自定义](customization.html)** — 更改颜色、侧边栏宽度、间距
- **[Migration from v3](migration.html)** — upgrading an existing AdminLTE 3 project
- **[从 v3 迁移](migration.html)** — 升级现有的 AdminLTE 3 项目
## Releases and changelog
## 版本发布和更新日志
Always grab the latest from the [GitHub releases page](https://github.com/ColorlibHQ/AdminLTE/releases) — that's where the changelog lives.
请始终从 [GitHub 发布页面](https://github.com/ColorlibHQ/AdminLTE/releases) 获取最新版本——更新日志就在那里。
Legacy releases
旧版本
Getting Started
入门指南
AdminLTE is an HTML template — there's no build step required to use it. This page gets you to a working dashboard in about ten minutes, without npm, without a build pipeline, without learning Astro.
AdminLTE 是一个 HTML 模板——使用它不需要任何构建步骤。本页将带您在十分钟内完成一个可用的仪表盘，无需 npm，无需构建流程，无需学习 Astro。
If you're integrating AdminLTE into a real project, the [Installation](introduction.html) page covers npm, Composer, and source-build workflows.
如果您正在将 AdminLTE 集成到实际项目中，[安装](introduction.html)页面涵盖了 npm、Composer 和源码构建工作流。
##### 1. Grab the CDN snippets
##### 1. 获取 CDN 代码片段
Copy these tags into the `<head>` of a new HTML file. They pull AdminLTE, Bootstrap 5.3, Popper, OverlayScrollbars, and Bootstrap Icons from jsDelivr — no install needed.
将这些标签复制到新 HTML 文件的 `<head>` 中。它们从 jsDelivr 拉取 AdminLTE、Bootstrap 5.3、Popper、OverlayScrollbars 和 Bootstrap Icons——无需安装。
And these just before `</body>`:
并将以下内容放在 `</body>` 之前：
##### 2. The minimum viable layout
##### 2. 最小可用布局
This is the smallest AdminLTE page that still feels like AdminLTE — a header, a collapsible sidebar, and a main content area. Drop it between the CSS and JS tags above:
这是最小的 AdminLTE 风格页面——包含头部、可折叠侧边栏和主内容区域。将其放在上述 CSS 和 JS 标签之间：
Open it in a browser. The hamburger button at the top-left collapses the sidebar — that's AdminLTE's [PushMenu](javascript/pushmenu.html) plugin working out of the box, wired by `data-lte-toggle="sidebar"`.
在浏览器中打开它。左上角的汉堡按钮可以折叠侧边栏——这就是 AdminLTE 的 [PushMenu](javascript/pushmenu.html) 插件开箱即用的效果，通过 `data-lte-toggle="sidebar"` 进行绑定。
##### 3. What's actually happening
##### 3. 实际原理
The four classes that do the heavy lifting:
承担主要工作的 4 个类：
| Class | What it does |
| 类名 | 作用 |
| `app-wrapper` | The CSS grid root — defines header / sidebar / main / footer regions |
| `app-wrapper` | CSS 网格根容器——定义页头/侧边栏/主内容/页脚区域 |
| `app-header` | Stuck to the top of the viewport when `layout-fixed` is on the body |
| `app-header` | 当 body 上添加 `layout-fixed` 时，固定在页面顶部 |
| `app-sidebar` | Collapsible side rail. Add `sidebar-expand-lg` to body to control breakpoint |
| `app-sidebar` | 可折叠侧边栏。在 body 上添加 `sidebar-expand-lg` 可控制不同屏幕下展开或折叠 |
| `app-main` | Scrollable content region — everything else lives here |
| `app-main` | 可滚动内容区域——内容部分都在这里 |
The [Layout](layout.html) page covers the full wrapper structure and the responsive options.
[布局](layout.html)页面涵盖了完整的包装器结构和响应式选项。
##### 4. Add a working JavaScript widget
##### 4. 添加一个可用的 JavaScript 小部件
AdminLTE ships seven JavaScript plugins. All of them are wired via `data-lte-*` attributes — no JavaScript code needed.
AdminLTE 内置了 7 个 JavaScript 插件。它们全部通过 `data-lte-*` 属性进行绑定——无需编写 JavaScript 代码。
Drop this in the content area to see the card-widget plugin in action (try the buttons in the card header):
将以下代码放入内容区，查看卡片插件的效果（试试卡片标题中的按钮）：
##### 5. Where to go next
##### 5. 接下来去哪里
You now have a working AdminLTE page. From here:
您现在已拥有一个可用的 AdminLTE 页面。接下来：
- **Make it pretty** → [Customization & Theming](customization.html) — change colors, fonts, and spacing via SCSS variables or CSS custom properties.
- **美化它** → [自定义与主题](customization.html) — 通过 SCSS 变量或 CSS 自定义属性更改颜色、字体和间距。
- **Make it functional** → [JavaScript Plugins](javascript/layout.html) — full reference for all seven plugins.
- **让它动起来** → [JavaScript 插件](javascript/layout.html) — 7 个插件的完整参考。
- **Make it dark** → [Color Mode](color-mode.html) — light/dark/auto toggle.
- **让它变暗** → [明暗模式](color-mode.html) — 亮色/暗色/自动切换。
- **Make it international** → [RTL Support](rtl.html) — right-to-left layouts for Arabic, Hebrew, Persian, etc.
- **让它国际化** → [RTL 支持](rtl.html) — 为阿拉伯语、希伯来语、波斯语等提供从右到左的布局。
- **Add real widgets** → [Recommended Integrations](integrations.html) — date pickers, multi-selects, charts, rich text editors. AdminLTE keeps its dep tree lean; this page tells you which third-party libraries to drop in.
- **添加真正的部件** → [推荐集成](integrations.html) — 日期选择器、多选、图表、富文本编辑器。AdminLTE 保持精简的依赖；本页告诉您需要引入哪些第三方库。
If you're upgrading an existing AdminLTE 3 project, see the [Migration from v3](migration.html) guide for breaking changes.
如果您正在升级现有 AdminLTE 3 项目，请查看[从 v3 迁移](migration.html)指南，了解不兼容的变更。
Every AdminLTE page follows the same structural blueprint: a wrapper that contains a header, a sidebar, a main content area, and (optionally) a footer. Understanding the four region classes is the difference between fighting the framework and using it productively.
每个 AdminLTE 页面都遵循相同的布局结构：一个包含页头、侧边栏、主内容区域和（可选）页脚的包装器。掌握这 4 个类的用法，是区分"踩坑"和"高效开发"的关键。
##### The four regions
##### 4 个区域
topbar / navbar
顶部栏 / 导航栏
page content
页面内容
The wrapper is a CSS grid container, the four children are grid areas. You don't have to know that to use it — the markup pattern alone is sufficient.
包装器是一个 CSS 网格容器，四个子元素是网格区域。您不需要了解这些就能使用它——仅凭标记模式就足够了。
##### Minimal markup
##### 最小标记
Order inside `.app-wrapper` doesn't matter — the grid template positions each child by its class. Use whatever order reads cleanly.
`.app-wrapper` 内部的顺序无关紧要——网格模板通过类名定位每个子元素。按您觉得清晰的方式排列即可。
##### Region reference
##### 区域参考
The root container. Defines the grid template that positions header, sidebar, main, and footer. Always lives directly inside `<body>`.
根容器。定义定位页头、侧边栏、主内容和页脚的网格模板。始终直接位于 `<body>` 内部。
You can add `.compact-mode` here (or anywhere up the tree) to reduce padding throughout the layout — useful for data-dense dashboards.
您可以在此处（或向上任意层级）添加 `.compact-mode` 来减少整个布局的内边距——适用于数据密集型的仪表盘。
The top bar. Use Bootstrap's navbar classes (`navbar navbar-expand bg-body`) for the visual. The header is where the sidebar toggle button lives — wire it with `data-lte-toggle="sidebar"`:
顶部栏。使用 Bootstrap 的导航栏类（`navbar navbar-expand bg-body`）来控制外观。侧边栏切换按钮位于页头中——使用 `data-lte-toggle="sidebar"` 进行绑定：
By default the header scrolls with the page. Add `.fixed-header` to `<body>` to pin it.
默认情况下，页头随页面滚动。在 `<body>` 上添加 `.fixed-header` 可将其固定。
The side rail. Always contains two children: `.sidebar-brand` (logo / app name) and `.sidebar-wrapper` (the scrollable nav region).
侧边栏。始终包含两个子元素：`.sidebar-brand`（Logo/应用名称）和 `.sidebar-wrapper`（可滚动的导航区域）。
The `data-bs-theme="dark"` on the sidebar gives you the dark-sidebar-on-light-page look from the demos. Drop it for a light sidebar.
侧边栏上的 `data-bs-theme="dark"` 可实现演示中亮色页面配暗色侧边栏的效果。移除它则可获得亮色侧边栏。
The page content region. Almost always contains two children:
页面内容区域。几乎总是包含两个子元素：
page body, cards, tables, charts, etc.
页面主体、卡片、表格、图表等。
`.app-content-header` is the title strip — typically an `<h3>` and a breadcrumb in a row. `.app-content` is everything below it. Both should wrap their children in `.container-fluid` (or `.container` for capped width).
`.app-content-header` 是标题区域——通常包含一个 `<h3>` 和面包屑导航。`.app-content` 是其下方的所有内容。两者都应将子元素包裹在 `.container-fluid` 中（或使用 `.container` 限制宽度）。
(optional)
(可选)
A footer strip at the bottom of the wrapper:
包装器底部的页脚区域：
Your Company
公司名称
Add `.fixed-footer` to `<body>` to pin it to the bottom of the viewport.
在 `<body>` 上添加 `.fixed-footer` 可将其固定在页面底部。
##### Body-level layout modifiers
##### Body 级布局修饰符
Layout behaviour is mostly controlled by classes on `<body>`, not `.app-wrapper`. The supported modifiers:
布局行为主要由 `<body>` 上的类控制，而不是 `.app-wrapper`。支持的修饰符：
| Class | What it does |
| 类名 | 作用 |
| `layout-fixed` | Sidebar gets its own scrollbar; only `.app-main` scrolls with the page |
| `layout-fixed` | 侧边栏拥有自己的滚动条；只有 `.app-main` 随页面滚动 |
| `fixed-header` | Sticky `.app-header` at the top; sidebar also pins (since v4.0.0) |
| `fixed-header` | 将 `.app-header` 固定在顶部；侧边栏也会固定（自 v4.0.0 起） |
| `fixed-footer` | Sticky `.app-footer` at the bottom |
| `fixed-footer` | 将 `.app-footer` 固定在底部 |
| `sidebar-expand-{sm,md,lg,xl,xxl}` | Breakpoint at which the sidebar transitions from off-canvas (mobile) to inline (desktop). `lg` is the default in the demos. |
| `sidebar-expand-{sm,md,lg,xl,xxl}` | 侧边栏从 off-canvas（移动端）切换为 inline（桌面端）的 breakpoint。演示中默认使用 `lg` |
| `sidebar-mini` | Collapses sidebar to icon-only mini state |
| `sidebar-mini` | 将侧边栏折叠为仅图标的小型状态 |
| `sidebar-mini sidebar-collapse` | Starts the page with the mini sidebar already collapsed |
| `sidebar-mini sidebar-collapse` | 页面加载时侧边栏已处于折叠的小型状态 |
| `sidebar-without-hover` | Disables the auto-expand-on-hover behaviour for the mini sidebar |
| `sidebar-without-hover` | 禁用迷你侧边栏的悬停自动展开行为 |
| `layout-rtl` | Mirrors the layout for right-to-left languages (also requires `.rtl.css` and `dir="rtl"` — see [RTL Support](rtl.html)) |
| `layout-rtl` | 为从右到左的语言镜像布局（还需要 `.rtl.css` 和 `dir="rtl"`——参见 [RTL 支持](rtl.html)） |
These compose. The demo dashboards use `class="layout-fixed sidebar-expand-lg bg-body-tertiary"` — fixed-height with a desktop sidebar breakpoint at the `lg` (≥992px) boundary.
这些类可以组合使用。演示仪表盘使用 `class="layout-fixed sidebar-expand-lg bg-body-tertiary"`——固定高度，桌面端侧边栏 breakpoint 为 `lg`（≥992px）。
###### Layout variants demo
###### 布局变体演示
The Layout section of the demo site (`/layout/*.html` files) ships eleven pre-built combinations:
演示站点的 Layout 部分（`/layout/*.html` 文件）提供了十一种预构建组合：
- [Fixed Sidebar](../layout/fixed-sidebar.html), [Fixed Header](../layout/fixed-header.html), [Fixed Footer](../layout/fixed-footer.html), [Fixed Complete](../layout/fixed-complete.html)
- [固定侧边栏](../layout/fixed-sidebar.html)、[固定页头](../layout/fixed-header.html)、[固定页脚](../layout/fixed-footer.html)、[全固定](../layout/fixed-complete.html)
- [Unfixed Sidebar](../layout/unfixed-sidebar.html), [Sidebar Mini](../layout/sidebar-mini.html), [Collapsed Sidebar](../layout/collapsed-sidebar.html), [Collapsed without hover](../layout/collapsed-sidebar-without-hover.html)
- [非固定侧边栏](../layout/unfixed-sidebar.html)、[迷你侧边栏](../layout/sidebar-mini.html)、[折叠侧边栏](../layout/collapsed-sidebar.html)、[无悬停折叠](../layout/collapsed-sidebar-without-hover.html)
- [Layout Custom Area](../layout/layout-custom-area.html), [Logo Switch](../layout/logo-switch.html), [Layout RTL](../layout/layout-rtl.html)
- [自定义区域布局](../layout/layout-custom-area.html)、[Logo 切换](../layout/logo-switch.html)、[RTL 布局](../layout/layout-rtl.html)
##### Responsive behaviour
##### 响应式行为
At and above your `sidebar-expand-*` breakpoint, the sidebar is **inline** — visible in the grid alongside `.app-main`. Below the breakpoint, it becomes **off-canvas** — hidden by default and slid in from the left when the user clicks the hamburger toggle.
在 `sidebar-expand-*` breakpoint 及以上，侧边栏为 **inline** 状态——在网格中与 `.app-main` 并排可见。低于 breakpoint 时，它变为 **off-canvas** 状态——默认隐藏，当用户点击汉堡菜单按钮时从左侧滑入。
Mobile-specific classes:
移动端专用类：
| Class | When applied | Effect |
| 类名 | 应用时机 | 效果 |
| `sidebar-open` | Body — when the user explicitly opens the mobile sidebar | Slides the sidebar in over the content with an overlay |
| `sidebar-open` | Body——当用户主动打开移动端侧边栏时 | 侧边栏带遮罩层从内容上方滑入 |
| `sidebar-collapse` | Body — set automatically when the user closes the mobile sidebar | Hides the sidebar |
| `sidebar-collapse` | Body——当用户关闭移动端侧边栏时自动设置 | 隐藏侧边栏 |
These are managed by the [PushMenu plugin](javascript/pushmenu.html) — you don't write them yourself.
这些由 [PushMenu 插件](javascript/pushmenu.html)管理——您无需自己编写。
##### Common pitfalls
##### 常见问题
- **Forgetting `.container-fluid` inside `.app-content`** — the content lands flush against the screen edge. Wrap your content cards in a container.
- **忘记在 `.app-content` 内部添加 `.container-fluid`** ——内容会紧贴屏幕边缘。请将内容卡片包裹在容器中。
- **Putting the sidebar toggle outside `.app-header`** — it'll still work, but the styling expects the navbar context.
- **将侧边栏切换按钮放在 `.app-header` 外部** ——它仍然可以工作，但样式预期是放在导航栏上下文中。
- **Mixing `.app-*` and `.main-*` classes** — `.main-*` is the AdminLTE 3 naming. Don't combine them. See [Migration from v3](migration.html).
- **混用 `.app-*` 和 `.main-*` 类** ——`.main-*` 是 AdminLTE 3 的命名方式。不要将它们组合使用。参见[从 v3 迁移](migration.html)。
- **Using `position: fixed` on something inside `.app-main`** — `layout-fixed` already gives `.app-main` `overflow: auto`. Children with `position: fixed` may behave unexpectedly. Use sticky positioning instead.
- **在 `.app-main` 内部的元素上使用 `position: fixed`** ——`layout-fixed` 已经为 `.app-main` 设置了 `overflow: auto`。使用 `position: fixed` 的子元素可能会出现意外行为。请改用粘性定位。
- **Hardcoding sidebar width in custom CSS** — the sidebar width is exposed via the SCSS variable `$lte-sidebar-width`. If you need to know it from JavaScript, read the rendered `.app-sidebar` element's computed width.
- **在自定义 CSS 中硬编码侧边栏宽度** ——侧边栏宽度通过 SCSS 变量 `$lte-sidebar-width` 暴露。如果需要在 JavaScript 中获取它，请读取渲染后的 `.app-sidebar` 元素的计算宽度。
##### Where to next
##### 接下来去哪里
- [Layout reference](layout.html) — the lower-level CSS classes and what they do
- [布局参考](layout.html) —— 更底层的 CSS 类及其作用
- [PushMenu plugin](javascript/pushmenu.html) — the sidebar toggle implementation
- [PushMenu 插件](javascript/pushmenu.html) —— 侧边栏切换的实现
- [Customization](customization.html) — how to change sidebar width, breakpoint, and colours
- [自定义](customization.html) —— 如何更改侧边栏宽度、breakpoint 和颜色
AdminLTE inherits Bootstrap 5.3's CSS-custom-properties theming model and adds its own layer of SCSS variables on top. You can customize the look two ways:
AdminLTE 继承了 Bootstrap 5.3 的 CSS 自定义属性主题模型，并在此基础上添加了自己的 SCSS 变量层。您可以通过两种方式自定义外观：
**CSS custom properties** — quick, no build step, works on prebuilt CSS.
**CSS 自定义属性** —— 快速，无需构建步骤，适用于预编译的 CSS。
**SCSS variables** — slower (requires a build), but lets you change anything including grid breakpoints and sidebar width.
**SCSS 变量** —— 较慢（需要构建），但允许您更改任何内容，包括网格 breakpoint 和侧边栏宽度。
##### Quick wins with CSS variables
##### 使用 CSS 变量的快速见效方式
Bootstrap and AdminLTE expose dozens of CSS custom properties on `:root`. Override them in your own stylesheet to retheme without rebuilding:
Bootstrap 和 AdminLTE 在 `:root` 上暴露了数十个 CSS 自定义属性。在您自己的样式表中覆盖主题样式，无需重新构建：
swap Bootstrap's blue for purple
将 Bootstrap 的蓝色替换为紫色
darker than Bootstrap's default
比 Bootstrap 的默认颜色更深
Components built on `var(--bs-primary)` (most of AdminLTE's chrome) pick this up automatically.
基于 `var(--bs-primary)` 构建的组件（AdminLTE 的大部分界面元素）会自动应用这些更改。
###### Common variables worth knowing
###### 值得了解的常用变量
| Variable | Default | What it controls |
| 变量 | 默认值 | 控制内容 |
| `--bs-primary` | `#0d6efd` | Brand colour, used in active sidebar items, primary buttons |
| `--bs-primary` | `#0d6efd` | 品牌色，用于激活的侧边栏项、主要按钮 |
| `--bs-body-bg` | `#fff` (light) / `#212529` (dark) | Page background |
| `--bs-body-bg` | `#fff`（亮色）/ `#212529`（暗色） | 页面背景 |
| `--bs-body-color` | `#212529` | Default text colour |
| `--bs-body-color` | `#212529` | 默认文字颜色 |
| `--bs-border-color` | `#dee2e6` | All hairline borders |
| `--bs-border-color` | `#dee2e6` | 所有细边框 |
| `--bs-border-radius` | `.375rem` | Default radius for cards, buttons, inputs |
| `--bs-border-radius` | `.375rem` | 卡片、按钮、输入框的默认圆角 |
| `--bs-border-radius-lg` | `.5rem` | Larger radius, used for modals |
| `--bs-border-radius-lg` | `.5rem` | 较大圆角，用于模态框 |
| `--bs-font-sans-serif` | `system-ui, ...` | Default font stack |
| `--bs-font-sans-serif` | `system-ui, ...` | 默认字体栈 |
For the full list, see [Bootstrap CSS variables](https://getbootstrap.com/docs/5.3/customize/css-variables/).
完整列表请参见 [Bootstrap CSS 变量](https://getbootstrap.com/docs/5.3/customize/css-variables/)。
##### SCSS variables
##### SCSS 变量
If you're building from source (cloned the repo or installed via npm + your own bundler), edit [`src/scss/_variables.scss`](https://github.com/ColorlibHQ/AdminLTE/blob/master/src/scss/_variables.scss) and recompile.
如果您是从源码构建（克隆了仓库或通过 npm + 自己的打包工具安装），请编辑 [`src/scss/_variables.scss`](https://github.com/ColorlibHQ/AdminLTE/blob/master/src/scss/_variables.scss) 并重新编译。
The most-overridden variables, with their defaults:
最常用的自定义变量及其默认值：
collapse to mini at ~70px
缩小至约 70px 的迷你尺寸
breakpoint at which sidebar starts off-canvas
侧边栏开始变为 off-canvas 模式的 breakpoint
// Sidebar colours (light theme)
// 侧边栏颜色（浅色主题）
// Sidebar
// 侧边栏
// Header
// 页眉
// Transitions
// 过渡
Bootstrap's own variables (button colours, font sizes, spacing scale, breakpoints) live in [`src/scss/_bootstrap-variables.scss`](https://github.com/ColorlibHQ/AdminLTE/blob/master/src/scss/_bootstrap-variables.scss). All values use `!default`, so override them in your own SCSS file *before* the import:
Bootstrap 自身的变量（按钮颜色、字体大小、间距比例、breakpoint）位于 [`src/scss/_bootstrap-variables.scss`](https://github.com/ColorlibHQ/AdminLTE/blob/master/src/scss/_bootstrap-variables.scss)。所有值都使用了 `!default`，因此在导入*之前*在您自己的 SCSS 文件中覆盖它们：
// 1. Your overrides FIRST
// 1. 你覆盖优先
// 2. Then import AdminLTE (which imports Bootstrap)
// 2. 然后导入 AdminLTE（它会导入 Bootstrap）
##### Recompiling SCSS
##### 重新编译 SCSS
If you cloned the repo, the build pipeline is already wired:
如果您克隆了仓库，构建流程已经配置好：
one-off build
一次性构建
rebuild on save during development
开发过程中保存时自动重新构建
For a project that uses AdminLTE as a dependency, point your bundler at `node_modules/admin-lte/src/scss/adminlte.scss`. Examples:
对于将 AdminLTE 作为依赖项的项目，将您的打包工具指向 `node_modules/admin-lte/src/scss/adminlte.scss`。示例：
##### Custom sidebar branding
##### 自定义侧边栏品牌标识
The `.sidebar-brand` element accepts text, an image, or both. Markup:
`.sidebar-brand` 元素可以包含文本、图片，或两者兼有。标记：
To swap the logo when the sidebar collapses to its mini state, AdminLTE provides a built-in pattern — see the [Logo Switch layout demo](../layout/logo-switch.html) for the markup.
要在侧边栏折叠为迷你状态时切换 Logo，AdminLTE 提供了内置模式——请查看 [Logo 切换布局演示](../layout/logo-switch.html) 了解标记方式。
##### Dark mode customization
##### 暗色模式自定义
AdminLTE follows Bootstrap 5.3's `data-bs-theme` attribute system. To customize dark-mode colours, scope your variables under `[data-bs-theme="dark"]`:
AdminLTE 遵循 Bootstrap 5.3 的 `data-bs-theme` 属性系统。要自定义暗色模式颜色，请将变量限定在 `[data-bs-theme="dark"]` 下：
lighter primary for dark backgrounds
浅色主色调适用于深色背景
The [Color Mode](color-mode.html) page covers the toggle markup and JavaScript that switches the attribute.
[颜色模式](color-mode.html)页面涵盖了切换该属性的切换按钮标记和 JavaScript。
##### Custom sidebar colour scheme
##### 自定义侧边栏配色方案
AdminLTE ships its sidebar styled for light mode, but the demos apply `data-bs-theme="dark"` on `.app-sidebar` to get the dark sidebar variant against a light page. You can mix and match:
AdminLTE 默认提供亮色模式的侧边栏样式，但演示中在 `.app-sidebar` 上应用了 `data-bs-theme="dark"`，以在亮色页面上获得暗色侧边栏变体。您可以自由组合：
Light page, dark sidebar (the demo pattern)
浅色页面，深色侧边栏（演示模式）
##### Compact mode
##### 紧凑模式
Add `.compact-mode` to `.app-wrapper` (or anywhere up the tree) to reduce padding throughout the layout — handy for data-dense dashboards. The dimensions for compact mode come from the `*-compact` variables in `_variables.scss`.
在 `.app-wrapper` 上添加 `.compact-mode`（或向上任意层级）可减少整个布局的内边距——适用于数据密集型的仪表盘。紧凑模式的尺寸来自 `_variables.scss` 中的 `*-compact` 变量。
header, sidebar, main content with tighter spacing
页眉、侧边栏和主要内容间距更紧密
##### When to recompile vs. override at runtime
##### 何时重新编译 vs. 运行时覆盖
| Customization | CSS variables | SCSS rebuild |
| 自定义项 | CSS 变量 | SCSS 重新编译 |
Brand colours
品牌色
Body / text colours
主体/文字颜色
Border radius
圆角
Sidebar width
侧边栏宽度
not a CSS var
不是 CSS 变量
Breakpoint changes
breakpoint 更改
Spacing scale
间距比例
| Font sizes | partial | ✅ (full control) |
| 字体大小 | 部分支持 | ✅（完全控制） |
Use CSS variables for everything you can — it survives AdminLTE upgrades without merge conflicts. Reserve SCSS overrides for structural changes (sidebar width, breakpoints) that CSS variables don't cover.
尽可能使用 CSS 变量进行所有自定义——这样可以在 AdminLTE 升级时避免合并冲突。将 SCSS 覆盖保留用于 CSS 变量无法覆盖的结构性更改（侧边栏宽度、breakpoint）。
AdminLTE ships an RTL (right-to-left) stylesheet alongside the standard LTR one, generated from the same source via [`rtlcss`](https://rtlcss.com/). Use it for Arabic, Hebrew, Persian, Urdu, and any other right-to-left scripts.
AdminLTE 在标准的 LTR（从左到右）样式表之外，还附带了一个 RTL（从右到左）样式表，通过 [`rtlcss`](https://rtlcss.com/) 从同一源码生成。用于阿拉伯语、希伯来语、波斯语、乌尔都语以及任何其他从右到左书写的语言。
##### The pre-built RTL stylesheet
##### 预构建的 RTL 样式表
Every release publishes RTL variants of every CSS file:
每个版本都会发布所有 CSS 文件的 RTL 变体：
(default)
(默认)
To switch a page to RTL, swap the stylesheet and set `dir="rtl"` on the `<html>` element:
要将页面切换为 RTL，请替换样式表并在 `<html>` 元素上设置 `dir="rtl"`：
That's it — the sidebar now docks to the right, scroll bars flip, padding/margin utilities mirror, and Bootstrap Icons stay LTR-safe (most icons aren't directional). For a live example, see [`layout/layout-rtl.html`](../layout/layout-rtl.html).
就这么简单——侧边栏现在靠右停靠，滚动条翻转，内边距/外边距工具类镜像，Bootstrap Icons 保持 LTR 安全（大多数图标没有方向性）。使用示例，请参见 [`layout/layout-rtl.html`](../layout/layout-rtl.html)。
##### Bootstrap's `dir="rtl"` attribute
##### Bootstrap 的 `dir="rtl"` 属性
Setting `dir="rtl"` triggers Bootstrap 5.3's logical property pathway as well — `.ms-*` becomes right-margin instead of left, `.float-start` floats right, etc. You don't need separate Bootstrap classes for RTL; the same utility names work in both directions.
设置 `dir="rtl"` 也会触发 Bootstrap 5.3 的逻辑属性路径——`.ms-*` 变为右边距而非左边距，`.float-start` 变为右浮动等等。您不需要为 RTL 单独使用不同的 Bootstrap 类；相同的工具类名称在两种方向下都有效。
##### When you have a mixed-language UI
##### 当您有混合语言 UI 时
If your app shows RTL content on an otherwise-LTR page (eg. an Arabic text block inside an English admin panel), you can scope `dir="rtl"` to a single element:
如果您的应用在 LTR 页面中显示 RTL 内容（例如英文管理面板中的阿拉伯语文本块），您可以将 `dir="rtl"` 限定在单个元素上：
Bootstrap and AdminLTE handle nested direction switches correctly — the inner element's direction is what counts.
Bootstrap 和 AdminLTE 能正确处理嵌套的方向切换——内部元素的方向优先。
##### Building RTL yourself
##### 自行构建 RTL
If you're building AdminLTE from source and you've modified the SCSS, regenerate the RTL CSS:
如果您从源码构建 AdminLTE 并修改了 SCSS，请重新生成 RTL CSS：
builds LTR
构建 LTR
adds autoprefixer
添加自动添加浏览器前缀
generates *.rtl.css from the LTR output
从 LTR 输出生成 *.rtl.css 文件
The full pipeline is wired into `npm run css`, so most of the time you just run that.
完整流程已集成到 `npm run css` 中，因此大多数情况下您只需运行该命令即可。
The conversion is handled by [`postcss`](https://postcss.org/) running [`rtlcss`](https://rtlcss.com/) — both are already in devDependencies. If you need to opt-out a specific rule from the LTR-to-RTL flip, use rtlcss control directives in your SCSS:
转换由运行 [`rtlcss`](https://rtlcss.com/) 的 [`postcss`](https://postcss.org/) 处理——两者都已在 devDependencies 中。如果您需要让某个特定规则不参与 LTR 到 RTL 的翻转，请在 SCSS 中使用 rtlcss 控制指令：
stays left-margin in both LTR and RTL
在左对齐和右对齐中都保持左缩进
##### What flips automatically
##### 自动翻转的内容
| Direction-aware property | Behaviour in RTL |
| 方向感知属性 | RTL 中的行为 |
Swapped to right
交换为右侧
| Swapped
| 交换
| Bootstrap `.ms-*`, `.me-*`, `.float-start`, `.float-end` | Mirror via Bootstrap's logical properties |
| Bootstrap `.ms-*`、`.me-*`、`.float-start`、`.float-end` | 通过 Bootstrap 的逻辑属性镜像 |
| `transform: translateX()` | Sign inverted |
| `transform: translateX()` | 符号反转 |
| Sidebar position | Docks to the right |
| 侧边栏位置 | 靠右停靠 |
| Dropdown menu open direction | Flips automatically (Bootstrap handles this) |
| 下拉菜单展开方向 | 自动翻转（由 Bootstrap 处理） |
##### What doesn't flip
##### 不翻转的内容
- Bootstrap Icons (SVG glyphs) — most icons are direction-neutral; arrows that *do* indicate direction (`bi-arrow-right`, `bi-chevron-right`, etc.) need to be manually swapped to their `-left` siblings.
- Bootstrap Icons（SVG 字形）——大多数图标是方向中性的；确实指示方向的箭头（`bi-arrow-right`、`bi-chevron-right` 等）需要手动替换为对应的 `-left` 版本。
- Inline images, charts, and third-party widgets — their internal layout doesn't react to `dir`. Check each integration (ApexCharts, FullCalendar, Tabulator) for an RTL option.
- 内联图片、图表和第三方小部件——它们的内部布局不会对 `dir` 做出响应。请检查每个集成（ApexCharts、FullCalendar、Tabulator）是否有 RTL 选项。
- Numbers — digits stay LTR even in RTL contexts (this is standard Unicode bidi behaviour, not a bug).
- 数字——即使在 RTL 上下文中，数字仍然保持 LTR（这是标准的 Unicode 双向文本行为，不是 Bug）。
##### Common gotchas
##### 常见问题
- **The `.min.css` minified RTL build is `.rtl.min.css`, not `.min.rtl.css`.** Filename order matters; the build script emits the former.
- **压缩后的 RTL 构建文件是 `.rtl.min.css`，而不是 `.min.rtl.css`。** 文件名顺序很重要；构建脚本生成的是前者。
- **Don't mix LTR and RTL stylesheets on the same page** — pick one. If you need a mixed-direction app, use `dir="rtl"` on the relevant subtree, not a separate stylesheet.
- **不要在同一页面上混合使用 LTR 和 RTL 样式表** —— 选择其一。如果您需要混合方向的应用，请在相关的子树上使用 `dir="rtl"`，而不是使用单独的样式表。
- **Icon mirroring**: when you swap an icon for RTL, also update its `aria-label` if it indicates direction (eg. "Next page" should still say "Next page" — only the glyph rotates).
- **图标镜像**：当您为 RTL 替换图标时，如果它指示方向，也要更新其 `aria-label`（例如 "Next page" 仍应显示为 "Next page" —— 只有字形发生变化）。
Copy-paste solutions for the patterns that show up in every admin dashboard. Each recipe assumes you've followed [Getting Started](getting-started.html) and have a working AdminLTE page to build on.
针对每个管理仪表盘中都会出现的模式，提供可直接复制粘贴的解决方案。每个方案都假定您已经按照[入门指南](getting-started.html)操作，并拥有一个可用的 AdminLTE 页面作为基础。
##### A multi-level sidebar with icons, badges, and headers
##### 带图标、徽章和标题的多级侧边栏
The demo dashboards use this exact pattern. Group sections with `.nav-header`, indicate item depth with nested `.nav-treeview` lists, and use `.navbar-badge` for counts.
演示仪表盘使用的就是这个模式。使用 `.nav-header` 对区域进行分组，使用嵌套的 `.nav-treeview` 列表表示项目层级深度，使用 `.navbar-badge` 显示计数。
To mark the current page as active, server-render the `.active` class onto the matching `.nav-link`. For SPA frameworks, add it on route change.
要将当前页面标记为激活状态，请在服务端将 `.active` 类渲染到对应的 `.nav-link` 上。对于 SPA 框架，在路由变更时添加它。
##### Highlighting the active sidebar item from JavaScript
##### 从 JavaScript 高亮当前侧边栏项
If you're rendering the sidebar once and want client-side route highlighting:
如果您是一次性渲染侧边栏，并希望客户端路由高亮：
Call on route change
路线变更通知
##### Responsive data tables
##### 响应式数据表格
Wrap a `<table>` in `.table-responsive` so it gets a horizontal scrollbar on narrow viewports instead of overflowing the card.
将 `<table>` 包裹在 `.table-responsive` 中，使其在窄视口下获得水平滚动条，而不是溢出卡片。
Recent orders
最近订单
For interactive sorting/filtering/pagination, use [Tabulator](integrations.html#tabulator) — `tables/data.html` in the demo has a full example.
如需交互式排序/筛选/分页，请使用 [Tabulator](integrations.html#tabulator)——演示中的 `tables/data.html` 有完整示例。
##### A user dropdown in the topbar
##### 顶部栏中的用户下拉菜单
>Profile
>资料
>Settings
>设置
Put this inside the `<ul class="navbar-nav ms-auto">` in `.app-header`.
将此代码放在 `.app-header` 中的 `<ul class="navbar-nav ms-auto">` 内部。
##### A KPI card (small-box)
##### KPI 卡片（small-box）
The bright-coloured stat boxes at the top of most dashboards:
大多数仪表盘顶部的彩色统计框：
repeat 3 more times with different values + colours
重复 3 次，使用不同的数值和颜色
Swap `text-bg-primary` for `text-bg-success`, `text-bg-warning`, `text-bg-danger`, etc.
将 `text-bg-primary` 替换为 `text-bg-success`、`text-bg-warning`、`text-bg-danger` 等。
##### A breadcrumb + page-title row
##### 面包屑 + 页面标题行
This is the standard `.app-content-header` pattern used across every demo page:
这是每个演示页面都使用的标准 `.app-content-header` 模式：
>Section
>章节
Current page
当前页
Page Title
页面标题
##### A timeline
##### 时间线
For activity feeds, audit logs, or roadmap views:
用于动态消息、审计日志或路线图视图：
sent you a message
给你发送一条消息
Hey, can we review the Q4 numbers tomorrow at 10?
嘿，我们明天10点能看一下第四季度的数字吗？
Read more
查看更多
The timeline component lives in `_timeline.scss` and has variants. See [`/UI/timeline.html`](../UI/timeline.html) for the full set.
时间线组件位于 `_timeline.scss` 中，并有多种变体。完整集合请参见 [`/UI/timeline.html`](../UI/timeline.html)。
##### A direct-chat widget
##### 直接聊天小部件
The chat-style card used for live customer support, team chat, etc. Includes contact toggle and message bubbles.
用于实时客户支持、团队聊天等的聊天风格卡片。包括联系人切换和消息气泡。
Hey, ready for the review?
嘿，准备好了吗？
Yes, on my way.
是的，我准备好了！
contacts list, shown when chat-pane toggled
聊天面板切换时显示的联系人列表
For a full-page chat application, see `/pages/chat.html` in the demo.
如需完整的聊天页面应用，请参见演示中的 `/pages/chat.html`。
##### Reacting to layout/sidebar changes
##### 响应布局/侧边栏变更
Listen for plugin events to coordinate with your own code — e.g., refit a chart when the sidebar collapses (so the chart re-measures its container width):
监听插件事件以与您自己的代码协调——例如，当侧边栏折叠时重新适配图表（使图表重新测量其容器宽度）：
your ApexCharts instance
ApexCharts 实例
The same pattern works for Tabulator, FullCalendar, and any other library that reads its container width on init.
同样的模式适用于 Tabulator、FullCalendar 以及任何其他在初始化时读取容器宽度的库。
##### Loading content into a card via fetch
##### 通过 fetch 将内容加载到卡片中
Loading&hellip;
加载中&hellip;
##### Confirming destructive actions before submission
##### 提交前确认破坏性操作
Bootstrap modal + a tiny confirmation pattern:
Bootstrap 模态框 + 一个简单的确认模式：
Delete account
删除账户
This permanently deletes your account and all associated data. This cannot be undone.
这将永久删除您的账户及所有相关数据，且无法恢复。
Yes, delete it
是的，删除它！
The [Color Mode](color-mode.html), [Accessibility](javascript/accessibility.html), and the bundled JS handle Escape-to-close, focus trap, and keyboard navigation inside the modal automatically.
[颜色模式](color-mode.html)、[无障碍访问](javascript/accessibility.html)以及捆绑的 JS 会自动处理模态框内的 Escape 关闭、焦点陷阱和键盘导航。
##### Where to next
##### 接下来去哪里
- [JavaScript Plugins Overview](javascript/plugins-overview.html) — every plugin's event names and methods
- [JavaScript 插件概述](javascript/plugins-overview.html) —— 每个插件的事件名称和方法
- [Recommended Integrations](integrations.html) — when you need more than what's in this list
- [推荐集成](integrations.html) —— 当您需要的超出此列表时
- [Customization](customization.html) — change colours, sidebar width, breakpoints
- [自定义](customization.html) —— 更改颜色、侧边栏宽度、breakpoint
AdminLTE ships seven JavaScript plugins as a single bundle (`adminlte.js`). Each one is exported from the package root, can be triggered via `data-lte-*` attributes for declarative use, and exposes a programmatic API for code-driven control.
AdminLTE 将 7 个 JavaScript 插件打包为一个单独的包（`adminlte.js`）。每个插件都从包根目录导出，可以通过 `data-lte-*` 属性以声明方式触发使用，并暴露出方法供代码驱动控制。
##### At a glance
##### 概览
[Reference]
 [参考]
auto-applied to
自动应用于
on parent menu
的父菜单上
| Plugin | Data API | Programmatic | Documentation |
| 插件 | Data 属性 | 方法 | 文档 |
##### Two ways to use them
##### 两种使用方式
###### 1. Data API (declarative)
###### 1. Data 属性（声明式）
For most pages, the data API is enough — no JavaScript code required. Drop the right `data-lte-*` attribute on the trigger element and the bundle wires it up on page load:
对于大多数页面，Data 属性就足够了——无需编写 JavaScript 代码。在触发元素上添加相应的 `data-lte-*` 属性，包会在页面加载时自动进行绑定：
Sidebar toggle
侧边栏切换
The bundle attaches all data-API listeners on `DOMContentLoaded`. Dynamically-injected elements still work for the [PushMenu](pushmenu.html), [CardWidget](card-widget.html), and [Treeview](treeview.html) plugins, which use event delegation.
该包在 `DOMContentLoaded` 事件上绑定所有 data-API 监听器。动态注入的元素仍然适用于 [PushMenu](pushmenu.html)、[CardWidget](card-widget.html) 和 [Treeview](treeview.html) 插件，因为它们使用事件委托。
###### 2. Programmatic (imperative)
###### 2. 程序化（命令式）
When you need to control a plugin from your own code — eg. open the sidebar after a successful login, or expand a card on a route change — instantiate the class directly:
当您需要从自己的代码中控制插件时——例如在登录成功后打开侧边栏，或在路由变更时展开卡片——直接实例化类即可：
bundler import
打包导入
Card maximize
卡片最大化
Or use the globals (UMD bundle, no build step):
或使用全局变量（UMD 包，无需构建步骤）：
The bundle assigns to window.adminlte
该捆绑包分配给 window.adminlte
##### Listening to plugin events
##### 监听插件事件
Every plugin fires a `CustomEvent` or `Event` on the element it operates on. Listen for these to coordinate with your own code:
每个插件都会在其操作的元素上触发 `CustomEvent` 或 `Event`。监听这些事件以与您自己的代码协调：
Card expanded:
卡片已展开：
user opened the sidebar — eg. analytics call
用户打开了侧边栏——例如分析调用
###### Event name reference
###### 事件名称参考
| Plugin | Event | Fired when |
| 插件 | 事件 | 触发时机 |
Sidebar expanded
侧边栏展开
Sidebar collapsed
侧边栏折叠 |
Submenu opened
子菜单打开
Submenu closed
子菜单关闭
Pre-opened submenu detected on page load
页面加载时检测到预先打开的子菜单
Card expanded
卡片展开
Card collapsed
卡片折叠
Card removed
卡片移除
Card maximized
卡片最大化
Card minimized
卡片最小化
Contacts pane opened
联系人面板打开
Contacts pane closed
联系人面板关闭
Entered fullscreen
进入全屏
Exited fullscreen
退出全屏
All events bubble. Attach listeners to `document` or `document.body` for global hooks, or to a specific card / sidebar element for scoped listeners.
所有事件都会冒泡。将监听器绑定到 `document` 或 `document.body` 上用于全局钩子，或绑定到特定的卡片/侧边栏元素上用于作用域监听。
##### Configuring via data attributes
##### 通过 data 属性进行配置
Some plugins read config from `data-*` attributes on their target element:
部分插件会从其目标元素的 `data-*` 属性中读取配置：
non-accordion (multiple submenus can be open at once)
非折叠式（可同时打开多个子菜单）
custom animation speed
自定义动画速度
Sidebar — opt into localStorage persistence (default: off) -->
侧边栏 — 启用本地存储持久化（默认：关闭）
Sidebar — override the mobile breakpoint
侧边栏 — 重写移动设备 breakpoint
Each plugin's reference page documents which attributes it supports.
每个插件的参考页面都记录了其支持的属性。
##### CSS classes the plugins manage
##### 插件管理的 CSS 类
The plugins toggle a small set of CSS classes that you can also style or react to:
插件会切换一小部分 CSS 类，您也可以对这些类进行样式设置或响应：
| Class | Set by | Where | Meaning |
| 类名 | 设置者 | 位置 | 含义 |
Sidebar collapsed (desktop mini state, or mobile-closed)
侧边栏已折叠（桌面迷你状态，或移动端关闭）
 Mobile sidebar explicitly opened by user
用户明确打开了移动端侧边栏
Mini-sidebar mode active
迷你侧边栏模式已激活
Submenu is currently expanded
子菜单当前已展开
Card body/footer are collapsed
卡片主体/页脚已折叠
`<html>` and `.card` | Card is in fullscreen mode
`<html>` 和 `.card` | 卡片处于全屏模式
Contacts pane visible
联系人面板可见
Transitions disabled briefly (during resize, etc.)
暂时禁用过渡动画（如调整大小时等）
Initial page-load animation finished
初始页面加载动画已完成
OS `prefers-reduced-motion` detected
检测到操作系统 `prefers-reduced-motion`
##### Production vs source
##### 生产环境 vs 源码
The plugins live in [`src/ts/`](https://github.com/ColorlibHQ/master/src/ts) as TypeScript modules. The published `dist/js/adminlte.js` is a Rollup bundle of all seven, exporting them under a single `adminlte` namespace (UMD) or named imports (ESM).
插件以 TypeScript 模块的形式位于 [`src/ts/`](https://github.com/ColorlibHQ/master/src/ts) 中。发布的 `dist/js/adminlte.js` 是所有 7 个插件的 Rollup 打包文件，在单个 `adminlte` 命名空间下导出（UMD）或以命名导入方式导出（ESM）。
If you only need one or two plugins and care about bundle size, importing individual modules from `node_modules/admin-lte/src/ts/` will tree-shake — but you'll need TypeScript in your toolchain.
如果您只需要其中一两个插件并且关注打包体积，从 `node_modules/admin-lte/src/ts/` 导入单个模块可以进行 tree-shake——但您的工具链中需要 TypeScript 支持。
##### Where to next
##### 接下来去哪里
- Detailed reference for each plugin (links in the table above)
- 每个插件的详细参考（上表中的链接）
- [Layout Blueprint](../layout-blueprint.html) — the structural classes the plugins operate on
- [布局结构](../layout-blueprint.html) —— 插件所操作的结构类
- [Accessibility](accessibility.html) — keyboard navigation, focus trapping, ARIA helpers
- [无障碍访问](accessibility.html) —— 键盘导航、焦点陷阱、ARIA 辅助
The Layout plugin manages CSS transitions during layout changes and signals when the app has finished loading.
Layout 插件负责管理布局变化期间的 CSS 过渡动画，并在应用加载完成时发出信号。
##### Behavior
##### 行为
The Layout plugin performs two key tasks automatically:
Layout 插件自动执行两项关键任务：
1. **Hold Transitions** - During window resize, CSS transitions on the sidebar, navbar, and content area are temporarily disabled to prevent visual glitches. The `hold-transition` class is added to `<body>` for the duration.
1. **暂停过渡动画** —— 在窗口大小调整期间，侧边栏、导航栏和内容区域的 CSS 过渡动画会被暂时禁用，以防止视觉闪烁。在此期间，`hold-transition` 类会被添加到 `<body>` 上。
2. **App Loaded Signal** - After a 400ms delay on DOMContentLoaded, the `app-loaded` class is added to `<body>`. Use this class in CSS to defer animations until the app is ready, preventing flicker on initial page load.
2. **应用加载完成信号** —— 在 DOMContentLoaded 事件触发后延迟 400ms，`app-loaded` 类会被添加到 `<body>` 上。在 CSS 中使用此类可将动画延迟到应用准备就绪之后，从而防止初始页面加载时的闪烁。
##### CSS Classes
##### CSS 类
| Class | Description |
| 类名 | 描述 |
Temporarily disables CSS transitions on layout elements. Applied during window resize.
暂时禁用布局元素上的 CSS 过渡动画。在窗口大小调整时应用。
Added to `<body>` after the initial layout is ready. Use for entrance animations.
在初始布局准备就绪后添加到 `<body>` 上。用于进场动画。
##### Usage in CSS
##### 在 CSS 中的用法
Hide sidebar transitions until the app is loaded
在应用加载完成前隐藏侧边栏过渡效果
Only animate after app is loaded
仅在应用加载后才动画化
##### Programmatic API
##### 调用方式
The Layout plugin self-initializes on `DOMContentLoaded` — there's nothing to wire up manually. The only public method is exposed for advanced cases where you want to suppress transitions around your own DOM mutations:
Layout 插件在 `DOMContentLoaded` 时自动初始化——无需手动绑定。唯一公开的方法适用于高级场景，当您希望在自己操作 DOM 变更时抑制过渡动画：
disable transitions for 200ms
禁用过渡效果 200 毫秒
do something that would otherwise animate awkwardly
执行某些操作，否则这些操作会产生生硬的动画
###### Methods
###### 方法
| Method | Returns | Description |
| 方法 | 返回值 | 描述 |
Adds `hold-transition` to `<body>`, removes it after `time` milliseconds (default `100`). Use to suppress CSS transitions during programmatic layout changes.
在 `<body>` 上添加 `hold-transition`，在 `time` 毫秒后移除（默认 `100`）。用于在程序化布局更改期间抑制 CSS 过渡动画。
##### Notes
##### 注意事项
- The `hold-transition` class is automatically removed after the timeout. You don't need to clean up.
- `hold-transition` 类会在超时后自动移除。您无需手动清理。
- No data attributes — the plugin reads no config and initialises with the body element on page load.
- 没有 data 属性 —— 该插件不读取任何配置，在页面加载时以 body 元素进行初始化。
- If you need to detect when the initial page-load animations are done, watch for the `app-loaded` class on `<body>` or listen for the standard `load` event.
- 如果您需要检测初始页面加载动画何时完成，请监听 `<body>` 上的 `app-loaded` 类，或监听标准的 `load` 事件。
The PushMenu plugin toggles the sidebar between expanded and collapsed states. It handles responsive behavior, optional localStorage persistence, and overlay management on mobile.
PushMenu 插件用于在侧边栏的展开和折叠状态之间进行切换。它处理响应式行为、可选的 localStorage 持久化存储，以及移动端遮罩层管理。
##### 用法
##### Usage
This plugin is activated via data attributes on the sidebar element and toggle buttons.
该插件通过侧边栏元素和切换按钮上的 data 属性激活。
**Data API**
**Data 属性**
Add `data-lte-toggle="sidebar"` to any button or link to toggle the sidebar.
在任何按钮或链接上添加 `data-lte-toggle="sidebar"` 即可切换侧边栏。
Toggle Sidebar
切换侧边栏
##### Configuration
##### 配置
Configure the sidebar via data attributes on the `.app-sidebar` element:
通过 `.app-sidebar` 元素上的 data 属性配置侧边栏：
| Attribute | Default | Description |
| 属性 | 默认值 | 描述 |
Screen width (px) below which the sidebar is considered mobile.
屏幕宽度（像素），低于此值时侧边栏被视为移动端模式。
Save sidebar state to localStorage across page loads.
将侧边栏状态保存到 localStorage，以便跨页面加载保持。
sidebar content
侧边栏内容
##### Responsive Behavior
##### 响应式行为
- **Desktop** (above breakpoint): Sidebar is expanded by default. Collapse is remembered if persistence is enabled.
- **桌面端**（高于 breakpoint）：侧边栏默认展开。如果启用了持久化存储，折叠状态会被记住。
- **Mobile** (at or below breakpoint): Sidebar is collapsed by default. An overlay appears when the sidebar is open, clicking it collapses the sidebar.
- **移动端**（低于或等于 breakpoint）：侧边栏默认折叠。当侧边栏打开时会出现遮罩层，点击遮罩层会折叠侧边栏。
##### CSS Classes
##### CSS 类
| Class | Applied to | Description |
| 类名 | 应用位置 | 描述 |
Sidebar is collapsed.
侧边栏已折叠。
Sidebar is explicitly open on mobile.
侧边栏在移动端已明确打开。
Enables mini sidebar mode (icons only when collapsed).
启用迷你侧边栏模式（折叠时仅显示图标）。
Prevents collapsed sidebar from expanding on hover.
防止折叠的侧边栏在悬停时展开。
Sets the responsive breakpoint (`sm`, `md`, `lg`, `xl`, `xxl`).
设置响应式 breakpoint（`sm`、`md`、`lg`、`xl`、`xxl`）。
##### Events
##### 事件
| Event | Description |
| 事件 | 描述 |
Fired when the sidebar is expanded.
侧边栏展开时触发。
Fired when the sidebar is collapsed.
侧边栏折叠时触发。
Both events bubble. Listen on `document` for a global hook:
两个事件都会冒泡。在 `document` 上监听以获取全局钩子：
Sidebar opened
侧边栏打开
##### Programmatic API
##### 调用方式
Import the class and instantiate it on any element (typically the sidebar toggle button or `document.body`):
导入类并在任何元素上实例化（通常是侧边栏切换按钮或 `document.body`）：
###### Methods
###### 方法
| Method | Returns | Description |
| 方法 | 返回值 | 描述 |
Expands or collapses the sidebar based on current state. Persists state if `enablePersistence` is `true`.
根据当前状态展开或折叠侧边栏。如果 `enablePersistence` 为 `true`，则持久化存储状态。
Expands the sidebar. Fires `open.lte.push-menu`.
展开侧边栏。触发 `open.lte.push-menu` 事件。
Collapses the sidebar. Fires `collapse.lte.push-menu`.
折叠侧边栏。触发 `collapse.lte.push-menu` 事件。
`true` if `<body>` has `sidebar-collapse` class.
如果 `<body>` 包含 `sidebar-collapse` 类，则返回 `true`。
`true` if `<body>` has `sidebar-open` (mobile, user opened it).
如果 `<body>` 包含 `sidebar-open`（移动端，用户主动打开），则返回 `true`。
`true` if `<body>` has `sidebar-mini`.
如果 `<body>` 包含 `sidebar-mini`，则返回 `true`。
`true` if `window.innerWidth ≤ sidebarBreakpoint`.
如果 `window.innerWidth ≤ sidebarBreakpoint`，则返回 `true`。
Manually save state to localStorage (`"sidebar-open"` or `"sidebar-collapse"`).
手动将状态保存到 localStorage（`"sidebar-open"` 或 `"sidebar-collapse"`）。
Read state from localStorage and apply it.
从 localStorage 读取状态并应用。
Remove saved state from localStorage.
从 localStorage 中移除已保存的状态。
Recompute correct state based on viewport size (collapse on mobile, expand on desktop unless mini). Called automatically on resize.
根据视口大小重新计算正确的状态（移动端折叠，桌面端展开，除非处于迷你模式）。在窗口调整大小时自动调用。
###### Storage key
###### 存储键名
When `enablePersistence` is enabled, the plugin reads/writes `localStorage.getItem("lte.sidebar.state")`. The stored value is one of:
当 `enablePersistence` 启用时，该插件读写 `localStorage.getItem("lte.sidebar.state")`。存储的值为以下之一：
- `"sidebar-open"` — user wants the sidebar expanded
- `"sidebar-open"` —— 用户希望侧边栏展开
- `"sidebar-collapse"` — user wants it collapsed
- `"sidebar-collapse"` —— 用户希望侧边栏折叠
##### 示例
##### Example
The Treeview plugin converts a nested list into a collapsible tree where submenus expand and contract on click. It's what powers the sidebar navigation in the demo dashboards.
Treeview 插件将嵌套列表转换为可折叠的树形结构，子菜单在点击时展开和收起。演示仪表板中的侧边栏导航就是由它驱动的。
##### Usage
##### 用法
###### Data API
###### Data 属性
Add `data-lte-toggle="treeview"` to the parent `<ul>` or `<ol>`. Inside, mark items that have children with `.nav-item` and put the submenu in a nested `<ul class="nav-treeview">`:
在父级 `<ul>` 或 `<ol>` 上添加 `data-lte-toggle="treeview"`。在其内部，使用 `.nav-item` 标记包含子项的条目，并将子菜单放在嵌套的 `<ul class="nav-treeview">` 中：
>Sales
>销售
>Traffic
>交易
>Dashboard
>仪表盘
Reports
报告
To start with a submenu pre-expanded, add `menu-open` to its `.nav-item`.
若要让子菜单在初始时已展开，请在其 `.nav-item` 上添加 `menu-open`。
##### Configuration
##### 配置
Configure via `data-*` attributes on the parent `<ul>`:
通过父级 `<ul>` 上的 `data-*` 属性进行配置：
| Attribute | Default | Description |
| 属性 | 默认值 | 描述 |
Close other open submenus when one opens. Set `false` for non-accordion behaviour (multiple submenus open at once).
当一个子菜单打开时，关闭其他已打开的子菜单。设为 `false` 可启用非手风琴模式（允许多个子菜单同时打开）。
Slide animation duration in milliseconds. Set `0` to disable animation.
滑动动画的持续时间（毫秒）。设为 `0` 可禁用动画。
Non-accordion mode: multiple submenus can stay open
非手风琴模式：多个子菜单可以保持打开状态
Faster animation
更快的动画
##### CSS Classes
##### CSS 类
| Class | Applied to | Description |
| 类名 | 应用于 | 描述 |
the parent of a submenu
子菜单的父级元素
The submenu is currently expanded.
表示该子菜单当前处于展开状态。
(nested)
（嵌套的）
Marks a submenu so the plugin recognises it.
标记子菜单，以便插件识别它。
`<i>` inside `.nav-link`
`.nav-link` 内的 `<i>`
The chevron icon that rotates when the submenu opens.
箭头图标，在子菜单打开时会旋转。
##### Events
##### 事件
| Event | Description |
| 事件 | 描述 |
Fired on the `.nav-item` when its submenu opens.
当子菜单打开时，在对应的 `.nav-item` 上触发。
Fired on the `.nav-item` when its submenu closes.
当子菜单关闭时，在对应的 `.nav-item` 上触发。
Fired once per page load for any submenu that's already expanded via the `menu-open` class.
在每次页面加载时，为所有已通过 `menu-open` 类展开的子菜单触发一次。
Opened submenu inside:
在内部打开子菜单：
##### Programmatic API
##### 调用方式
// expand this item's submenu
// 展开项目子菜单
// collapse it
// 收起
// toggle
// 切换
###### Methods
###### 方法
| Method | Returns | Description |
| 方法 | 返回值 | 描述 |
Expands the submenu. If `accordion` is `true`, closes sibling submenus first. Fires `expanded.lte.treeview`.
展开子菜单。如果 `accordion` 为 `true`，则先关闭同级子菜单。触发 `expanded.lte.treeview` 事件。
Collapses the submenu. Fires `collapsed.lte.treeview`.
收起子菜单。触发 `collapsed.lte.treeview` 事件。
Calls `open()` or `close()` based on current state.
根据当前状态调用 `open()` 或 `close()`。
##### Example
##### 示例
One level
一级
>Level
>层级
>Multilevel
>多层级
The CardWidget plugin provides collapse, expand, remove, and maximize functionality for card components.
CardWidget 插件为卡片组件提供了折叠、展开、移除和最大化的功能。
##### Usage
##### 用法
This plugin is activated via data attributes on buttons inside a `.card` element.
该插件通过 `.card` 元素内部按钮上的 data 属性来激活。
**Data API**
**Data 属性**
Add `data-lte-toggle="card-collapse"` to toggle card body visibility, `data-lte-toggle="card-remove"` to remove the card, or `data-lte-toggle="card-maximize"` to maximize it.
添加 `data-lte-toggle="card-collapse"` 来切换卡片主体的可见性，添加 `data-lte-toggle="card-remove"` 来移除卡片，或添加 `data-lte-toggle="card-maximize"` 来最大化卡片。
"Collapse"
"折叠"
"Maximize"
"最大化"
"Remove"
"移除"
Card Title
卡片标题
Card content here.
这里是卡片内容。
Card footer
卡片底部
##### Events
##### 事件
The plugin dispatches the following events on the trigger element:
该插件在触发元素上派发以下事件：
| Event | Description |
| 事件 | 描述 |
Fired when the card body is collapsed.
当卡片主体被折叠时触发。
Fired when the card body is expanded.
当卡片主体被展开时触发。
Fired when the card is removed.
当卡片被移除时触发。
Fired when the card is maximized.
当卡片被最大化时触发。
Fired when the card is minimized (restored from maximized).
当卡片被最小化时（从最大化状态恢复）触发。
##### CSS Classes
##### CSS 类
| Class | Description |
| 类名 | 描述 |
Applied to the card when collapsed.
当卡片折叠时应用于卡片。
Applied to the card when maximized.
当卡片最大化时应用于卡片。
##### Configuration
##### 配置
The plugin reads no `data-*` configuration attributes. To customize behaviour, instantiate it programmatically and pass a config:
该插件不读取 `data-*` 配置属性。要自定义行为，请通过编程方式实例化并传入配置对象：
| Option | Default | Description |
| 选项 | 默认值 | 描述 |
Slide animation duration in milliseconds.
滑动动画的持续时间（毫秒）。
CSS selector for collapse buttons.
折叠按钮的 CSS 选择器。
CSS selector for remove buttons.
移除按钮的 CSS 选择器。
CSS selector for maximize buttons.
最大化按钮的 CSS 选择器。
##### Programmatic API
##### 调用方式
// expand to fullscreen
// 展开至全屏
// hide body and footer
// 隐藏主体和页脚
// restore from collapsed
// 从折叠状态恢复
// animate out and remove from DOM
// 以动画方式移出并从 DOM 中删除
// expand / collapse based on current state
// 根据当前状态展开/折叠
// maximize / minimize based on current state
// 根据当前状态最大化/最小化
###### Methods
###### 方法
| Method | Returns | Description |
| 方法 | 返回值 | 描述 |
Slides up body and footer, adds `collapsed-card`. Fires `collapsed.lte.card-widget`.
向上滑动收起主体和页脚，添加 `collapsed-card` 类。触发 `collapsed.lte.card-widget` 事件。
Slides down body and footer, removes `collapsed-card`. Fires `expanded.lte.card-widget`.
向下滑动展开主体和页脚，移除 `collapsed-card` 类。触发 `expanded.lte.card-widget` 事件。
Calls `collapse()` or `expand()` based on the `collapsed-card` class.
根据 `collapsed-card` 类的存在情况调用 `collapse()` 或 `expand()`。
Sets the card to fixed fullscreen positioning, adds `maximized-card`. Fires `maximized.lte.card-widget`.
将卡片设置为固定全屏定位，添加 `maximized-card` 类。触发 `maximized.lte.card-widget` 事件。
Restores original size, removes `maximized-card`. Fires `minimized.lte.card-widget`.
恢复原始尺寸，移除 `maximized-card` 类。触发 `minimized.lte.card-widget` 事件。
Calls `maximize()` or `minimize()` based on the `maximized-card` class.
根据 `maximized-card` 类的存在情况调用 `maximize()` 或 `minimize()`。
Animates the card out and removes it from the DOM. Fires `remove.lte.card-widget`.
以动画方式移除卡片并将其从 DOM 中删除。触发 `remove.lte.card-widget` 事件。
##### Notes
##### 注意事项
- Nested cards are handled correctly: collapsing a parent card does not affect child cards.
- 嵌套卡片会被正确处理：折叠父级卡片不会影响子卡片。
- Maximizing a card also adds `maximized-card` to `<html>` so global styles can react.
- 最大化卡片时也会给 `<html>` 添加 `maximized-card` 类，以便全局样式可以做出响应。
- A card that's collapsed when you maximize it remembers that — the `was-collapsed` class tracks this state.
- 如果在最大化时卡片处于折叠状态，它会记住这一状态——`was-collapsed` 类用于跟踪此状态。
The DirectChat plugin toggles the contacts pane in a direct chat widget.
DirectChat 插件用于切换直接聊天小组件中的联系人面板。
##### Usage
##### 用法
This plugin is activated via data attributes.
该插件通过 data 属性激活。
**Data API**
**Data 属性**
Add `data-lte-toggle="chat-pane"` to a button inside a `.direct-chat` container to toggle the contacts list.
在 `.direct-chat` 容器内的按钮上添加 `data-lte-toggle="chat-pane"` 来切换联系人列表。
Chat messages go here
聊天消息在这里
Contacts list goes here
联系人列表在这里
Toggle Contacts
切换联系人
Direct Chat
聊天
##### Events
##### 事件
| Event | Description |
| 事件 | 描述 |
Fired when the contacts pane is opened.
当联系人面板打开时触发。
Fired when the contacts pane is closed.
当联系人面板关闭时触发。
##### CSS Classes
##### CSS 类
| Class | Description |
| 类名 | 描述 |
Applied to the `.direct-chat` container when the contacts pane is visible.
当联系人面板可见时，应用于 `.direct-chat` 容器。
##### Programmatic API
##### 调用方式
show or hide the contacts pane
显示或隐藏联系人面板
###### Methods
###### 方法
| Method | Returns | Description |
| 方法 | 返回值 | 描述 |
Toggles the `direct-chat-contacts-open` class on the `.direct-chat` container. Fires `expanded.lte.direct-chat` or `collapsed.lte.direct-chat`.
切换 `.direct-chat` 容器上的 `direct-chat-contacts-open` 类。触发 `expanded.lte.direct-chat` 或 `collapsed.lte.direct-chat` 事件。
The constructor takes no config object — pass only the `.direct-chat` element.
该构造函数不接受配置对象——只需传入 `.direct-chat` 元素即可。
The FullScreen plugin toggles the browser's native fullscreen mode.
FullScreen 插件用于切换浏览器的原生全屏模式。
##### Usage
##### 用法
This plugin is activated via data attributes.
该插件通过 data 属性激活。
**Data API**
**Data 属性**
Add `data-lte-toggle="fullscreen"` to a button element. Use `data-lte-icon="maximize"` and `data-lte-icon="minimize"` on child icon elements to toggle their visibility.
在按钮元素上添加 `data-lte-toggle="fullscreen"`。在子级图标元素上使用 `data-lte-icon="maximize"` 和 `data-lte-icon="minimize"` 来切换它们的可见性。
##### Events
##### 事件
| Event | Description |
| 事件 | 描述 |
Fired when entering fullscreen mode.
进入全屏模式时触发。
Fired when exiting fullscreen mode.
退出全屏模式时触发。
##### Programmatic API
##### 调用方式
// enter or exit based on current state
// 根据当前状态进入或退出全屏
// request fullscreen
// 请求进入全屏
// exit fullscreen
// 退出全屏
###### Methods
###### 方法
| Method | Returns | Description |
| 方法 | 返回值 | 描述 |
Calls `inFullScreen()` if not in fullscreen, otherwise `outFullscreen()`. Checks `document.fullscreenEnabled` first.
如果未处于全屏模式则调用 `inFullScreen()`，否则调用 `outFullscreen()`。首先检查 `document.fullscreenEnabled`。
Calls `document.documentElement.requestFullscreen()`, toggles `.d-none` on the maximize/minimize icons. Fires `maximized.lte.fullscreen`.
调用 `document.documentElement.requestFullscreen()`，切换最大化/最小化图标上的 `.d-none` 类。触发 `maximized.lte.fullscreen` 事件。
Calls `document.exitFullscreen()`, toggles `.d-none` on the maximize/minimize icons. Fires `minimized.lte.fullscreen`.
调用 `document.exitFullscreen()`，切换最大化/最小化图标上的 `.d-none` 类。触发 `minimized.lte.fullscreen` 事件。
The constructor takes no config — pass only the trigger element.
该构造函数不接受配置——只需传入触发元素即可。
##### Notes
##### 注意事项
- Requires browser support for the [Fullscreen API](https://developer.mozilla.org/docs/Web/API/Fullscreen_API). Check `document.fullscreenEnabled` before invoking.
- 需要浏览器支持 [Fullscreen API](https://developer.mozilla.org/docs/Web/API/Fullscreen_API)。在调用之前请检查 `document.fullscreenEnabled`。
- The maximize icon is hidden via the `d-none` Bootstrap utility class (not inline `display`) so it doesn't override the icon library's natural display value — this works correctly with FontAwesome, Lucide, Tabler Icons, etc.
- 最大化图标通过 Bootstrap 工具类 `d-none` 来隐藏（而非内联 `display` 样式），这样不会覆盖图标库的原生 display 值——这适用于 FontAwesome、Lucide、Tabler Icons 等图标库。
- Fullscreen on `document.documentElement` (i.e. `<html>`) means the entire page goes fullscreen. To fullscreen just a card, use the [CardWidget `maximize()` method](card-widget.html#programmatic-api) instead.
- 对 `document.documentElement`（即 `<html>`）应用全屏意味着整个页面进入全屏模式。如果只想全屏显示单个卡片，请改用 [CardWidget 的 `maximize()` 方法](card-widget.html#programmatic-api)。
The Accessibility module provides WCAG 2.1 AA compliance features that are automatically initialized when AdminLTE loads.
Accessibility 模块提供了 WCAG 2.1 AA 合规性功能，在 AdminLTE 加载时会自动初始化。
##### Features
##### 功能特性
| Feature | WCAG Criterion | Description |
| 功能 | WCAG 标准 | 描述 |
| Skip Links | 2.4.1 Bypass Blocks | Adds "Skip to main content" and "Skip to navigation" links. |
| 跳过链接 | 2.4.1 绕过区块 | 添加“跳转到主要内容”和“跳转到导航”链接。 |
| Focus Management | 2.4.3 & 2.4.7 | Tab cycling, focus trapping in modals, and focus restoration. |
| 焦点管理 | 2.4.3 和 2.4.7 | Tab 键循环、模态框中的焦点锁定以及焦点恢复。 |
| Keyboard Navigation | 2.1.1 Keyboard | Arrow key navigation in menus, Enter/Space for custom buttons. |
| 键盘导航 | 2.1.1 键盘 | 菜单中的方向键导航，自定义按钮的 Enter/Space 键支持。 |
| Reduced Motion | 2.3.3 Animation | Respects `prefers-reduced-motion` by disabling animations. |
| 减少动画 | 2.3.3 动画 | 通过禁用动画来尊重 `prefers-reduced-motion` 偏好设置。 |
| Live Announcements | 4.1.3 Status Messages | Announces errors and success messages to screen readers. |
| 实时通告 | 4.1.3 状态消息 | 向屏幕阅读器通告错误和成功消息。 |
| Error Identification | 3.3.1 & 3.3.2 | Auto-labels unlabelled inputs, announces form validation errors. |
| 错误识别 | 3.3.1 和 3.3.2 | 自动为无标签的输入添加标签，通告表单验证错误。 |
| Table Accessibility | 1.3.1 Info & Relationships | Adds `scope` attributes to table headers automatically. |
| 表格无障碍 | 1.3.1 信息和关系 | 自动为表格表头添加 `scope` 属性。 |
##### Configuration
##### 配置
The module is initialized in `adminlte.ts` with all features enabled by default:
该模块在 `adminlte.ts` 中初始化，默认启用所有功能：
Set any option to `false` to disable that feature.
将任意选项设为 `false` 即可禁用该功能。
##### Public API
##### 调用方式
// Announce a message to screen readers
// 向屏幕阅读器通告消息
Item saved successfully
项目已成功保存
Form has errors
表单有错误
// Focus a specific element
// 聚焦特定元素
// Trap focus inside a container (useful for custom modals/dialogs)
// 将焦点锁定在容器内（适用于自定义模态框/对话框）
// Add semantic landmarks to the page
// 为页面添加语义化的地标
##### Utility Functions
##### 工具函数
// Check color contrast ratio (WCAG 1.4.3)
// 检查颜色对比度（WCAG 1.4.3）
// Check if an element is focusable
// 检查元素是否可聚焦
// Generate a unique ID for ARIA attributes
// 为 ARIA 属性生成唯一 ID
##### CSS Classes
##### CSS 类
| Class | Description |
| 类名 | 描述 |
Container for skip navigation links.
跳过导航链接的容器。
Individual skip link, visible only on focus.
单个跳过链接，仅在聚焦时可见。
Added to `<body>` when user prefers reduced motion.
当用户偏好减少动画时，添加到 `<body>` 上。
Screen reader only content (visually hidden).
仅屏幕阅读器可见的内容（视觉上隐藏）。
ARIA live region for dynamic announcements.
用于动态通告的 ARIA 实时区域。
##### Notes
##### 注意事项
- The module integrates with Bootstrap's modal and dropdown events for focus management.
- 该模块与 Bootstrap 的模态框和下拉菜单事件集成，用于焦点管理。
- Modal Escape key handling is deferred to Bootstrap to respect `keyboard: false` and stacked modals.
- 模态框的 Escape 键处理交由 Bootstrap 处理，以尊重 `keyboard: false` 设置和堆叠模态框。
- Form validation announcements work automatically for any input with HTML5 validation attributes.
- 表单验证通告会自动适用于任何带有 HTML5 验证属性的输入框。
AdminLTE deliberately ships a lean dependency tree. Rather than bundling every form widget or chart library, we recommend best-in-class third-party libraries you can drop in when you need them. Every library below is **MIT-licensed**, **actively maintained**, and **jQuery-free**.
AdminLTE 有意保持精简的依赖树。我们不捆绑每一个表单组件或图表库，而是推荐一流的第三方库，你可以在需要时随时引入。下面的每个库都是 **MIT 许可**、**积极维护**且 **不依赖 jQuery** 的。
Each section has a CDN-based "install" snippet and the minimum JavaScript needed to wire it up. You can swap CDN URLs for npm installs if you're using a bundler.
每个部分都包含一个基于 CDN 的“安装”代码片段以及连接所需的最少 JavaScript。如果你使用打包工具，可以将 CDN URL 替换为 npm 安装方式。
> **About the version numbers below:** every CDN URL pins a specific version (eg. `@4.6.13`) so the snippets stay reproducible. They were current as of the latest release, but third-party libraries publish new versions independently of AdminLTE. Before copying a snippet into production, check the library's homepage (linked in each section) for the latest version — and consider hosting a copy yourself rather than relying on the CDN long-term.
> **关于以下版本号：** 每个 CDN URL 都固定了特定版本（例如 `@4.6.13`），以确保代码片段可复现。这些版本在最新发布时是有效的，但第三方库会独立于 AdminLTE 发布新版本。在将代码片段复制到生产环境之前，请查看每个部分中链接的库主页以获取最新版本——并考虑自行托管副本，而非长期依赖 CDN。
##### Form widgets
##### 表单组件
###### Date / time picker — Flatpickr
###### 日期/时间选择器 — Flatpickr
[Flatpickr](https://flatpickr.js.org/) is a lightweight, no-dependency date and time picker.
[Flatpickr](https://flatpickr.js.org/) 是一个轻量级、无依赖的日期和时间选择器。
Select a date
选择日期
Supports date ranges, time-only mode, multiple dates, and i18n out of the box.
开箱即用地支持日期范围、仅时间模式、多日期选择和国际化。
###### Multi-select / tag input — Tom Select
###### 多选/标签输入 — Tom Select
[Tom Select](https://tom-select.js.org/) is a vanilla-JS rewrite of Selectize. Great fit for searchable selects, tag inputs, and remote-data autocomplete.
[Tom Select](https://tom-select.js.org/) 是 Selectize 的原生 JS 重写版本。非常适合可搜索下拉框、标签输入和远程数据自动完成。
Pick some tags
选择标签
>Design
>设计
>Development
>开发
>QA
>质量保证
Ships a `tom-select.bootstrap5.min.css` theme that matches Bootstrap form controls.
附带 `tom-select.bootstrap5.min.css` 主题，与 Bootstrap 表单控件样式匹配。
###### Range slider — noUiSlider
###### 范围滑块 — noUiSlider
[noUiSlider](https://refreshless.com/nouislider/) handles single, range, and multi-handle sliders with no dependencies.
[noUiSlider](https://refreshless.com/nouislider/) 处理单值、范围和多手柄滑块，无依赖。
###### Color picker — Pickr
###### 颜色选择器 — Pickr
[Pickr](https://github.com/Simonwep/pickr) is a flat, modern color picker with multiple themes.
[Pickr](https://github.com/Simonwep/pickr) 是一个扁平、现代的颜色选择器，支持多种主题。
###### Input mask — IMask
###### 输入掩码 — IMask
[IMask](https://imask.js.org/) handles phone numbers, credit cards, dates, currency — anything with a fixed pattern.
[IMask](https://imask.js.org/) 处理电话号码、信用卡、日期、货币——任何具有固定格式的内容。
##### File handling
##### 文件处理
###### File uploader — Dropzone
###### 文件上传器 — Dropzone
[Dropzone](https://www.dropzone.dev/) is the de-facto drag-and-drop uploader. The v6 rewrite is plain JavaScript (the older v5 had jQuery-coupled examples; v6+ does not).
[Dropzone](https://www.dropzone.dev/) 是事实上的拖拽上传器。v6 重写版是纯 JavaScript（旧版 v5 有与 jQuery 耦合的示例；v6+ 没有）。
###### File uploader — FilePond
###### 文件上传器 — FilePond
[FilePond](https://pqina.nl/filepond/) is a more modern alternative with a polished UX and a plugin system for image preview, validation, EXIF, etc.
[FilePond](https://pqina.nl/filepond/) 是一个更现代的替代方案，具有精致的用户体验和用于图像预览、验证、EXIF 等的插件系统。
##### Rich text & markdown editors
##### 富文本和 Markdown 编辑器
###### Rich text — Quill
###### 富文本 — Quill
[Quill](https://quilljs.com/) is the best-supported open-source WYSIWYG editor. Lightweight, modular, no dependencies.
[Quill](https://quilljs.com/) 是支持最好的开源所见即所得编辑器。轻量、模块化、无依赖。
Write something fancy&hellip;
写点有趣的东西&hellip;
[EasyMDE](https://github.com/Ionaru/easy-markdown-editor) is the actively-maintained fork of SimpleMDE. Same look, same API, but still gets updates.
[EasyMDE](https://github.com/Ionaru/easy-markdown-editor) 是 SimpleMDE 的积极维护分支。相同的界面，相同的 API，但仍持续获得更新。
[Toast UI Editor](https://ui.toast.com/tui-editor) supports both markdown and WYSIWYG modes side-by-side. Best when you want users to pick their preferred mode.
[Toast UI Editor](https://ui.toast.com/tui-editor) 同时支持 Markdown 和 WYSIWYG 模式并排显示。适合希望用户自行选择偏好模式的场景。
##### Charts
##### 图表
###### ApexCharts
###### ApexCharts
[ApexCharts](https://apexcharts.com/) is the chart library used in the AdminLTE dashboards. Modern look, animated, MIT.
[ApexCharts](https://apexcharts.com/) 是 AdminLTE 仪表板中使用的图表库。现代外观、带动画、MIT 许可。
"area"
"区域"
"Visits"
"访问量"
"Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"
"周一", "周二", "周三", "周四", "周五", "周六", "周日"
[Chart.js](https://www.chartjs.org/) is the most-asked-for traditional chart library. Canvas-based, very wide chart-type support.
[Chart.js](https://www.chartjs.org/) 是被问及最多的传统图表库。基于 Canvas，支持非常广泛的图表类型。
"Q1", "Q2", "Q3", "Q4"
"一季度", "二季度", "三季度", "四季度"
"Revenue"
"收入"
##### Data tables
##### 数据表格
[Tabulator](https://tabulator.info/) is the jQuery-free DataTables alternative used in `tables/data.html`. Sort, filter, paginate, edit, export — all built in.
[Tabulator](https://tabulator.info/) 在 `tables/data.html` 中使用无需 jQuery 的 DataTables 替代方案。排序、筛选、分页、编辑、导出——全部内置。
"Name"
"名称"
"Role"
"职位"
##### Calendar
##### 日历
[FullCalendar](https://fullcalendar.io/) is the calendar used in `pages/calendar.html`. Drag-and-drop scheduling, multiple views, recurrence, MIT.
[FullCalendar](https://fullcalendar.io/) 是 [`/pages/calendar.html`](../pages/calendar.html) 中使用的日历。拖拽调度、多视图、重复事件、MIT 许可。
Team standup
团队晨会
##### Drag and drop
##### 拖拽
[SortableJS](https://sortablejs.github.io/Sortable/) powers the kanban board demo. Reorder lists, drag between lists, touch-friendly. 31k stars.
[SortableJS](https://sortablejs.github.io/Sortable/) 为看板演示提供支持。重新排序列表、在列表间拖拽、触控友好。31k Star。
>Item
>项目
##### Lightbox / image viewer
##### Lightbox / 图像查看器
[GLightbox](https://biati-digital.github.io/glightbox/) is a vanilla-JS lightbox for images, videos, and inline content.
[GLightbox](https://biati-digital.github.io/glightbox/) 是一个原生 JS Lightbox，用于图像、视频和内联内容。
Click to enlarge
点击放大
##### Icon libraries
##### 图标库
AdminLTE ships [Bootstrap Icons](https://icons.getbootstrap.com/) by default — they're SVG-based, ~1,900 icons, and used throughout the demo. If you need more, these all work alongside AdminLTE:
AdminLTE 默认附带 [Bootstrap Icons](https://icons.getbootstrap.com/) ——它们是基于 SVG 的，约 1,900 个图标，并在整个演示中使用。如果你需要更多，以下库都可以与 AdminLTE 配合使用：
| Library | Icons | Notes |
| 库 | 图标数量 | 备注 |
Default,
默认，
Modern,
现代，
very consistent style
风格非常统一
broadest free set available
可用的最广泛免费图标集
variable font
可变字体
free | Most-recognized, font-based
免费 | 最知名，基于字体
six weights
六种字体效果
Mix and match — they all coexist.
可以混用——它们都能共存。
##### A note on bundling
##### 关于打包的说明
The examples above use jsDelivr CDNs for copy-paste convenience. For production use, prefer:
以上示例使用 jsDelivr CDN 是为了方便复制粘贴。在生产环境中，建议：
**npm install + bundler** (Vite, Webpack, Rollup) — best for tree-shaking and version locking
**npm install + 打包工具**（Vite、Webpack、Rollup）——最适合 tree-shaking 和版本锁定
**Self-host** the minified bundles — avoids CDN downtime as a failure mode
**自托管** 压缩后的 bundle——避免 CDN 宕机成为故障模式
**CDN with `integrity` hashes** — fine for prototypes and demos
**带 `integrity` 哈希的 CDN**——适用于原型和演示
AdminLTE doesn't ship any of these libraries — keeping the framework's footprint small is intentional. Pick what you need, skip what you don't.
AdminLTE 不附带这些库中的任何一个——保持框架体积小巧是刻意为之。按需选用，不需不用。
AdminLTE 4 is a ground-up rewrite. It targets Bootstrap 5.3, drops jQuery, ships as TypeScript-authored vanilla JS, and reorganises the CSS class structure. If you're upgrading an AdminLTE 3 project, the changes below are the ones most likely to bite.
AdminLTE 4 是一次从头开始的重写。它基于 Bootstrap 5.3，移除了 jQuery，以 TypeScript 编写的原生 JavaScript 发布，并重新组织了 CSS 类结构。如果你正在升级 AdminLTE 3 项目，以下变更是最可能带来影响的。
##### The high-level summary
##### 概要一览
CSS framework
CSS 框架
**Vanilla TypeScript only**
**仅原生 TypeScript**
attribute (Bootstrap-native)**
属性（Bootstrap 原生）**
Browser support
浏览器支持
Separate `rtl.css` build
独立的 `rtl.css` 构建
**Generated automatically via rtlcss**
**通过 rtlcss 自动生成**
Build tooling
构建工具
Icon library
图标库
(Lucide / Tabler Icons documented as alternatives)
（Lucide / Tabler Icons 作为替代方案已文档化）
If your project relies on jQuery or IE 11, stay on AdminLTE 3 — those constraints are not reversible in v4.
如果你的项目依赖 jQuery 或 IE 11，请继续使用 AdminLTE 3——这些限制在 v4 中不可逆。
##### Class renames
##### 类名重命名
The most common find-and-replace work:
最常见的查找替换工作：
(on the parent menu)
（在父级菜单上）
(removed — Bootstrap offcanvas replaces this)
（已移除——由 Bootstrap offcanvas 替代）
on `<body>`
在 `<body>` 上
on any element
（可在任意元素上）
##### Bootstrap 4 → 5 utility renames
##### Bootstrap 4 → 5 工具类重命名
These come from Bootstrap itself, not AdminLTE, but they affect every page:
这些来自 Bootstrap 本身，而非 AdminLTE，但它们会影响每个页面：
A regex find-replace handles most of this in a few minutes. The migration tool [`bootstrap-5-migration`](https://github.com/dbtek/bootstrap-5-migration) automates the bulk.
使用正则表达式查找替换可以在几分钟内处理大部分内容。迁移工具 [`bootstrap-5-migration`](https://github.com/dbtek/bootstrap-5-migration) 可以自动完成批量替换。
##### jQuery removal
##### jQuery 移除
If your app code calls AdminLTE 3 plugins via jQuery (`$('.sidebar').PushMenu()`, etc.), those calls no longer work. Replacements:
如果你的应用代码通过 jQuery 调用 AdminLTE 3 插件（例如 `$('.sidebar').PushMenu()` 等），这些调用将不再有效。替代方案：
the data-API does this for you
data 属性会为你完成这项工作
If you need programmatic control, import the plugin class directly:
如果需要编程式控制，直接导入插件类：
All seven JS plugins (`Layout`, `CardWidget`, `Treeview`, `DirectChat`, `FullScreen`, `PushMenu`, plus `initAccessibility`) are exported from `admin-lte`.
所有 7 个 JS 插件（`Layout`、`CardWidget`、`Treeview`、`DirectChat`、`FullScreen`、`PushMenu`，以及 `initAccessibility`）均从 `admin-lte` 导出。
##### Dark mode
##### 深色模式
AdminLTE 3 used a `.dark-mode` class toggled on the `<body>`. AdminLTE 4 uses Bootstrap 5.3's native `data-bs-theme="dark"` attribute, which can be applied at any level — `<html>`, `<body>`, or a single component.
AdminLTE 3 使用在 `<body>` 上切换的 `.dark-mode` 类。AdminLTE 4 使用 Bootstrap 5.3 原生的 `data-bs-theme="dark"` 属性，该属性可以应用于任何层级——`<html>`、`<body>` 或单个组件。
The included [Color Mode](color-mode.html) widget reads/writes this attribute and persists the user's choice in `localStorage`. See the [Color Mode](color-mode.html) docs page for the toggle implementation.
内置的 [Color Mode](color-mode.html) 组件读取/写入此属性，并将用户的选择保存在 `localStorage` 中。有关切换实现，请参阅 [Color Mode](color-mode.html) 文档页面。
##### CSS variables instead of `.bg-*` overrides
##### 使用 CSS 变量替代 `.bg-*` 重写类
AdminLTE 3 shipped a lot of one-off colour classes (`.bg-navy`, `.bg-purple`, `.bg-fuchsia`, etc.). In v4, prefer Bootstrap 5.3's CSS-variable model:
AdminLTE 3 包含大量一次性的颜色类（如 `.bg-navy`、`.bg-purple`、`.bg-fuchsia` 等）。在 v4 中，推荐使用 Bootstrap 5.3 的 CSS 变量模型：
use Bootstrap utilities + CSS variables
使用 Bootstrap 工具 + CSS 变量，data 属性会为你自动处理
For full retheming, override `--bs-primary` etc. on `:root`. See [Customization](customization.html).
如需完整重新主题，请在 `:root` 上覆盖 `--bs-primary` 等变量。参见 [Customization](customization.html)。
##### Icons
##### 图标
AdminLTE 3 demos used FontAwesome 6 free. AdminLTE 4 standardises on [Bootstrap Icons](https://icons.getbootstrap.com/) — they're SVG, lighter, MIT-licensed, and ship with Bootstrap's ecosystem.
AdminLTE 3 演示使用 FontAwesome 6 免费版。AdminLTE 4 标准化使用 [Bootstrap Icons](https://icons.getbootstrap.com/)——它们是 SVG 格式、更轻量、MIT 许可，并与 Bootstrap 生态一同发布。
If you'd rather stick with FontAwesome (or use Lucide, Tabler Icons, Material Symbols, etc.), nothing in AdminLTE 4 forces Bootstrap Icons — load whichever icon font you prefer and replace the `<i>` classes. See [Recommended Integrations](integrations.html#icon-libraries) for the comparison table.
如果你更愿意继续使用 FontAwesome（或使用 Lucide、Tabler Icons、Material Symbols 等），AdminLTE 4 中没有任何内容强制使用 Bootstrap Icons——加载你喜欢的任何图标字体并替换 `<i>` 类即可。参见 [Recommended Integrations](integrations.html#icon-libraries) 的对比表。
##### Pages we haven't ported yet
##### 尚未移植的页面
The v3 demo includes pages that haven't all been recreated for v4. The v4 release notes cover the current page catalog. If you depend on a specific v3 page that hasn't been ported, please [open an issue](https://github.com/ColorlibHQ/AdminLTE/issues) — community contributions for missing pages are very welcome.
v3 演示中包含的页面尚未全部为 v4 重新创建。v4 发布说明中涵盖了当前的页面目录。如果你依赖某个尚未移植的特定 v3 页面，请 [提交 issue](https://github.com/ColorlibHQ/AdminLTE/issues)——非常欢迎社区为缺失的页面贡献代码。
##### Suggested migration order
##### 建议的迁移顺序
For an existing AdminLTE 3 project:
对于现有的 AdminLTE 3 项目：
**Branch first.** This is not a small change — keep `master` on v3 until v4 is stable.
**先创建分支。** 这不是一个小改动——在 v4 稳定之前，保持 `master` 分支在 v3 上。
**Update CSS class names** with a find-replace pass using the table above. Most layouts compile but look broken until step 3.
**更新 CSS 类名**，使用上面的表格进行查找替换。大多数布局可以编译，但在第 3 步之前看起来是错乱的。
**Update `data-toggle` → `data-bs-toggle`** etc. across all your templates.
**更新 `data-toggle` → `data-bs-toggle`** 等，覆盖所有模板。
**Replace `.dark-mode` toggle code** with `data-bs-theme` attribute writes.
**将 `.dark-mode` 切换代码**替换为 `data-bs-theme` 属性写入。
**Audit jQuery calls.** Either remove them (use the data API) or import plugin classes directly.
**审查 jQuery 调用。** 要么移除它们（使用 data 属性），要么直接导入插件类。
**Test responsive breakpoints** — Bootstrap 5 added `xxl`. If you use breakpoint-specific utilities, double-check the new scale.
**测试响应式 breakpoint**——Bootstrap 5 新增了 `xxl`。如果你使用了 breakpoint 特定的工具类，请仔细检查新的尺寸体系。
**Re-test print views** — AdminLTE 4 fixed a print-layout issue (#5996); if you had custom print CSS, verify it still works.
**重新测试打印视图**——AdminLTE 4 修复了打印布局问题（#5996）；如果你有自定义打印 CSS，请验证其仍然有效。
##### Help and discussion
##### 帮助与讨论
- Migration questions: [GitHub Discussions](https://github.com/ColorlibHQ/AdminLTE/discussions)
- 迁移问题：[GitHub 讨论](https://github.com/ColorlibHQ/AdminLTE/discussions)
- Found something we missed? [Open an issue](https://github.com/ColorlibHQ/AdminLTE/issues) tagged `migration`.
- 发现我们遗漏了什么？[提交 issue](https://github.com/ColorlibHQ/AdminLTE/issues) 并标记 `migration`。
AdminLTE is a static HTML/CSS/JS template — there's nothing exotic about deploying it. But there are a handful of choices that meaningfully change page weight, time-to-interactive, and CDN reliability. This page documents the ones worth getting right.
AdminLTE 是一个静态 HTML/CSS/JS 模板——部署它并没有什么特别之处。但有一些选择会显著影响页面体积、可交互时间和 CDN 可靠性。本页记录了这些值得正确处理的事项。
##### What to deploy
##### 部署什么
The deployable artefact is everything under `dist/` plus your own HTML / server-rendered templates:
可部署的产物是 `dist/` 下的所有内容，加上你自己的 HTML/服务端渲染模板：
stylesheets (LTR + RTL, dev + minified)
样式表（LTR + RTL，开发版 + 压缩版）
JavaScript bundle (dev + minified)
JavaScript 打包文件（开发版 + 压缩版）
demo images / fonts (replace with your own)
演示图片 / 字体（请替换为您的内容）
You don't ship `src/`, `node_modules/`, or the demo HTML if you're embedding AdminLTE into your own app.
如果你将 AdminLTE 嵌入到自己的应用中，则不需要发布 `src/`、`node_modules/` 或演示 HTML。
##### Always use the `.min` variants in production
##### 生产环境始终使用 `.min` 压缩版本
The minified builds are the same code with whitespace stripped and identifiers shortened. For `adminlte.css` the saving is roughly 15-20%; for `adminlte.js` it's bigger (about 30% after gzip). The non-minified files are useful in development for readable source-map debugging — never serve them to users.
压缩版本是相同的代码，只是去除了空白字符并缩短了标识符。对于 `adminlte.css`，节省大约 15-20%；对于 `adminlte.js`，节省更大（gzip 后约 30%）。未压缩文件在开发环境中对于可读的 source-map 调试很有用——切勿将它们提供给用户。
| Asset | Dev | Production |
| 资源 | 开发环境 | 生产环境 |
| Stylesheet
| 样式表
| Script
| 脚本
| RTL stylesheet
| RTL 样式表
##### Compression at the edge
##### 边缘压缩
Make sure your web server / CDN gzips (or brotlis) responses for the relevant content types:
确保你的 Web 服务器/CDN 对相关内容类型启用 gzip（或 brotli）压缩：
Cloudflare, Vercel, Netlify, and most managed hosting do this automatically. Verify with `curl -I -H "Accept-Encoding: gzip" your-page-url` and look for `content-encoding: gzip` in the response.
Cloudflare、Vercel、Netlify 以及大多数托管服务都会自动执行此操作。使用 `curl -I -H "Accept-Encoding: gzip" your-page-url` 验证，并在响应中查找 `content-encoding: gzip`。
##### Cache headers
##### 缓存头
The dist artefacts have content hashes in their filenames *only if you wire it up via your bundler* — AdminLTE itself ships fixed filenames. Two reasonable strategies:
构建产物的文件名是否包含内容哈希，*取决于您是否通过构建工具进行配置*——AdminLTE 默认使用固定文件名。以下是两种合理的做法：
**Short cache + version query string** (simple, works without a build pipeline):
**短缓存 + 版本查询字符串**（简单，无需构建流程即可工作）：
Set `Cache-Control: public, max-age=86400` (1 day) at the server. Bump `?v=` on every release to force cache invalidation.
在服务器上设置 `Cache-Control: public, max-age=86400`（1 天）。每次发布时更新 `?v=` 以强制缓存失效。
**Fingerprinted filenames** (production-grade, requires a bundler):
**指纹文件名**（生产级，需要打包工具）：
Let your bundler emit `adminlte.[hash].min.css` and reference that hash in your HTML. Set `Cache-Control: public, max-age=31536000, immutable` (1 year). This is what Vite/Webpack/Rollup do by default if you import AdminLTE through them.
让你的打包工具生成 `adminlte.[hash].min.css` 并在 HTML 中引用该哈希值。设置 `Cache-Control: public, max-age=31536000, immutable`（1 年）。如果你通过 Vite/Webpack/Rollup 导入 AdminLTE，它们默认就是这样做的。
##### CDN integrity hashes
##### CDN 完整性哈希
If you load AdminLTE from a public CDN (jsDelivr, unpkg), include a Subresource Integrity (SRI) hash so a compromised CDN can't serve malicious JavaScript silently:
如果你从公共 CDN（jsDelivr、unpkg）加载 AdminLTE，请包含子资源完整性（SRI）哈希，以防止被入侵的 CDN 静默提供恶意 JavaScript：
You can get the hash from the [jsDelivr file detail page](https://www.jsdelivr.com/package/npm/admin-lte) — click any file and copy the "SRI" line. Do the same for Bootstrap, Popper, OverlayScrollbars, and Bootstrap Icons.
你可以从 [jsDelivr 文件详情页](https://www.jsdelivr.com/package/npm/admin-lte)获取哈希值——点击任意文件并复制 "SRI" 行。对 Bootstrap、Popper、OverlayScrollbars 和 Bootstrap Icons 也要做同样的操作。
For private-hosted assets, generate the hash yourself:
对于自托管的资源，自行生成哈希：
##### Loading order matters
##### 加载顺序很重要
Get the script order right or things break silently:
脚本顺序一定要放对，否则有些功能会悄无声息地失效：
Bootstrap Icons (font CSS) — load first so icons render with the page
Bootstrap Icons（字体 CSS）——首先加载，以便图标随页面一起渲染
OverlayScrollbars (optional, for the sidebar scroller)
OverlayScrollbars（可选，用于侧边栏滚动）
AdminLTE — includes Bootstrap CSS via its imports
AdminLTE——通过其导入包含 Bootstrap CSS
Scripts at the END of body
脚本放在 body 末尾
Popper (Bootstrap dependency for dropdowns/tooltips/popovers)
Popper（Bootstrap 下拉菜单/工具提示/弹出框的依赖）
AdminLTE — must load AFTER Bootstrap
AdminLTE——必须在 Bootstrap 之后加载
##### Defer or async?
##### Defer 还是 async？
The AdminLTE bundle waits for `DOMContentLoaded` before wiring its data API, so `defer` is safe and recommended:
AdminLTE 包会在 `DOMContentLoaded` 之后再挂载其 data API，因此 `defer` 是安全且推荐的：
Don't use `async` — the script could execute before its dependencies (Bootstrap, Popper) have parsed, and listeners would attach to elements that don't exist yet.
不要使用 `async`——脚本可能会在其依赖（Bootstrap、Popper）解析完成之前执行，而监听器会附加到尚不存在的元素上。
##### Critical CSS
##### 关键 CSS
For sub-1-second first paint, inline the styles that the above-the-fold layout uses (header + brand + first visible content) and load the rest of `adminlte.min.css` asynchronously:
为了实现 1 秒以内的首次绘制，将首屏布局使用的样式（页眉 + 品牌 + 首个可见内容）内联，并异步加载其余的 `adminlte.min.css`：
extracted critical CSS for /dashboard
为 /dashboard 提取的关键 CSS
Extract critical CSS with a tool like [Critters](https://github.com/GoogleChromeLabs/critters) or [Penthouse](https://github.com/pocketjoso/penthouse) as part of your build. For dashboards behind a login (where first-load speed matters less than for marketing pages), this is usually overkill.
使用 [Critters](https://github.com/GoogleChromeLabs/critters) 或 [Penthouse](https://github.com/pocketjoso/penthouse) 等工具提取关键 CSS，作为构建流程的一部分。对于登录后的仪表板（首屏加载速度的重要性不如营销页面），这通常是多此一举。
##### Preload key assets
##### 预加载关键资源
Push critical resources to the browser before it discovers them in the HTML:
在浏览器从 HTML 中发现关键资源之前，将其推送至浏览器：
Only preload what's used on every page. Preloading per-route assets in a shared layout wastes bandwidth.
只预加载每个页面都会用到的资源。在共享布局中预加载路由级资源会浪费带宽。
##### Lazy-loading sub-pages
##### 子页面懒加载
If your dashboard has many routes and the JS is server-rendered HTML per route, prefer per-route CSS imports rather than one mega-stylesheet. AdminLTE's core CSS is small (~44KB gzipped) but page-specific styles (charts, calendars, tables) can add up — only load them where they're used.
如果你的仪表板有很多路由，且 JS 是每个路由的服务端渲染 HTML，建议使用按路由导入 CSS，而非一个巨大的样式表。AdminLTE 的核心 CSS 很小（~44KB gzip），但页面特定样式（图表、日历、表格）会累积——只在需要的地方加载它们。
For SPAs, code-split heavy integrations:
对于 SPA，对重量级集成进行代码拆分：
Don't bundle the calendar app into the main chunk
不要将日历应用打包到主 chunk 中
##### Bundle size budget
##### 打包体积预算
The current published `dist/` artefacts gzip to:
当前发布的 `dist/` 产物 gzip 后的大小：
| Asset | Size (gzip) |
| 资源 | 大小（gzip） |
The full first-page payload (HTML + Bootstrap + AdminLTE + Bootstrap Icons + Popper + OverlayScrollbars) is around **180 KB gzipped** when loaded from CDN. The CI build enforces a `bundlewatch` check that fails if any asset crosses its budget.
完整的首屏负载（HTML + Bootstrap + AdminLTE + Bootstrap Icons + Popper + OverlayScrollbars）从 CDN 加载时约为 **180 KB gzip**。CI 构建会强制执行 `bundlewatch` 检查，如果任何资源超出预算则会失败。
##### Service workers / offline support
##### Service Worker / 离线支持
AdminLTE is just static files — caching them in a service worker works without changes. A minimal Workbox config:
AdminLTE 只是静态文件——将其缓存在 service worker 中无需任何修改即可工作。一个最小化的 Workbox 配置：
...your own pages
...你自己的页面
##### Removing dev-only files before deploy
##### 部署前移除开发专用文件
If you're deploying the demo `dist/` folder verbatim, strip these patterns from the upload:
如果你原样部署演示 `dist/` 文件夹，请从上传内容中剔除以下模式：
(source maps — leak source code to anyone who opens devtools)
（source map——会将源代码泄露给任何打开 devtools 的人）
(the Astro intermediate build, if present)
（Astro 中间构建产物，如果存在）
(Astro cache)
（Astro 缓存）
For `rclone` / `rsync`:
对于 `rclone` / `rsync`：
##### A note on Cloudflare cache
##### 关于 Cloudflare 缓存的说明
Cloudflare's default Browser Cache TTL is 4 hours. If you push a new release and want users to see it immediately, [purge the affected paths](https://developers.cloudflare.com/cache/how-to/purge-cache/) from the Cloudflare dashboard or API. Otherwise, users on cached pages will see your old CSS / JS for up to four hours.
Cloudflare 的默认浏览器缓存 TTL 为 4 小时。如果你发布了新版本并希望用户立即看到，请从 Cloudflare 仪表板或 API [清除受影响的路径](https://developers.cloudflare.com/cache/how-to/purge-cache/)。否则，访问缓存页面的用户将在最多四小时内看到旧的 CSS/JS。
##### Where to next
##### 接下来
- [Customization](customization.html) — change colours, sidebar width, breakpoints before you deploy
- [定制](customization.html)——在部署前更改颜色、侧边栏宽度、breakpoint
- [Recommended Integrations](integrations.html) — pick lightweight libraries for the things AdminLTE doesn't bundle
- [推荐集成](integrations.html)——为 AdminLTE 未打包的功能选择轻量级库
- [Accessibility](javascript/accessibility.html) — make sure your deployed pages keep their a11y guarantees
- [无障碍访问](javascript/accessibility.html)——确保你部署的页面保持其无障碍访问
AdminLTE comes with the same browser support as Bootstrap 5.
AdminLTE 拥有与 Bootstrap 5 相同的浏览器支持。
> ##### Excerpt from Bootstrap's official Documentation!
> ##### 摘自 Bootstrap 官方文档！
> Bootstrap supports the **latest, stable releases** of all major browsers and platforms.
> Bootstrap 支持所有主流浏览器及平台的**最新稳定版本**。
> For more details [look here](https://getbootstrap.com/docs/5.0/getting-started/browsers-devices/#supported-browsers).
> 更多详情请[查看此处](https://getbootstrap.com/docs/5.0/getting-started/browsers-devices/#supported-browsers)。
You can find our supported range of browsers and their versions in [our .browserslistrc file](https://github.com/ColorlibHQ/AdminLTE/blob/master/.browserslistrc):
您可以在[我们的 .browserslistrc 文件](https://github.com/ColorlibHQ/AdminLTE/blob/master/.browserslistrc)中找到我们支持的浏览器及其版本范围：
last 2 major versions
最近 2 个主要版本
not dead
非弃用（仍在活跃使用）
not Explorer <= 11
不支持 Explorer <= 11
# Contributing to AdminLTE
# 参与 AdminLTE 贡献
Contributions are always **welcome and recommended**! Here is how for beginner's: [Get started with open source click here](https://youtu.be/GbqSvJs-6W4)
我们始终**欢迎并推荐**大家贡献代码！初学者请参考这里：[点击此处开始开源之旅](https://youtu.be/GbqSvJs-6W4)
1. Contribution Requirements :
1. 贡献要求：
* When you contribute, you agree to give a non-exclusive license to AdminLTE.io to use that contribution in any context as we (AdminLTE.io) see appropriate.
* 当您贡献代码时，即表示您同意授予 AdminLTE.io 非独占许可，允许我们（AdminLTE.io）在任何我们认为合适的场景中使用该贡献。
* If you use content provided by another party, it must be appropriately licensed using an [open source](https://opensource.org/licenses) license.
* 如果您使用了第三方提供的内容，则必须通过[开源](https://opensource.org/licenses)许可证进行适当授权。
* Contributions are only accepted through GitHub pull requests.
* 贡献仅通过 GitHub Pull Request 方式接受。
* Finally, contributed code must work in all supported browsers (see above for browser support).
* 最后，贡献的代码必须在所有支持的浏览器中正常工作（浏览器支持请参见上文）。
2. Installation :
2. 安装步骤：
* Fork the repository ([here is the guide](https://help.github.com/articles/fork-a-repo/)).
* Fork 该仓库（[指南参见此处](https://help.github.com/articles/fork-a-repo/)）。
* Clone to your machine
* 克隆到本地
* Create a new branch from `master`
* 从 `master` 分支创建一个新分支
Compile dist files (Development) :
编译 dist 文件（开发环境）：
* To compile the dist files you need Node.js 18 or higher/npm (node package manager)
* 编译 dist 文件需要 Node.js 18 或更高版本以及 npm（Node 包管理器）
* `npm install` (install npm deps)
* `npm install`（安装 npm 依赖）
* `npm run dev` (developer mode, autocompile with browsersync support for live demo)
* `npm run dev`（开发者模式，自动编译并支持 Browsersync 实时演示）
* Make your changes only in `./src` Folder OR `package.json` in any files which are necessary for contribution
* 请仅在 `./src` 文件夹或 `package.json` 中对贡献所需的文件进行修改
* Do not make changes in `./dist/**` Because it contains compiled files and do not include in PR (Pull Request)
* 请勿在 `./dist/**` 中进行修改，因为该目录包含编译后的文件，且不应包含在 PR（Pull Request）中
* `npm run production` (compile css/js files and test all pages are perfectly working fine, before creating a pull request)
* `npm run production`（编译 CSS/JS 文件，并在创建 Pull Request 之前测试所有页面是否完全正常工作）
Create a pull request to `master` branch
向 `master` 分支发起 Pull Request
## Online one-click setup for contributing
## 一键在线贡献环境搭建
You can use [Codespace](https://docs.github.com/en/codespaces) an online IDE which is free for Open Source for working on issues or making PRs (Pull Requests). With a single click it will launch a workspace and automatically:
您可以使用 [Codespace](https://docs.github.com/en/codespaces)（一个在线 IDE，对开源项目免费）来处理 Issue 或发起 PR（Pull Request）。只需单击一次，它就会启动一个工作空间，并自动：
- clone the `AdminLTE` repo.
- 克隆 `AdminLTE` 仓库。
- Open with [Codespace](https://docs.github.com/en/codespaces) or [![Open in Gitpod](https://gitpod.io/button/open-in-gitpod.svg)](https://gitpod.io/from-referrer/)
- 通过 [Codespace](https://docs.github.com/en/codespaces) 打开，或 [![Open in Gitpod](https://gitpod.io/button/open-in-gitpod.svg)](https://gitpod.io/from-referrer/)
- install the dependencies.
- 安装依赖。
- run `npm run dev` to start the server.
- 运行 `npm run dev` 启动服务器。
AdminLTE is an open source project that is licensed under the [MIT license](https://opensource.org/licenses/MIT). This allows you to do pretty much anything you want as long as you include the copyright in "all copies or substantial portions of the Software." Attribution is not required (though very much appreciated).
AdminLTE 是一个开源项目，基于 [MIT 许可证](https://opensource.org/licenses/MIT) 发布。只要在“软件的所有副本或实质性部分”中保留版权声明，您几乎可以做任何您想做的事情。不要求署名（尽管非常欢迎）。
<h5 class="text-bold text-dark mt-3">What You Are <span class="text-success">Allowed</span> To Do With AdminLTE</h5>
<h5 class="text-bold text-dark mt-3"><span class="text-success">允许</span>使用 AdminLTE 做什么 </h5>
- Use in commercial projects.
- 用于商业项目。
- Use in personal/private projects.
- 用于个人/私人项目。
- Modify and change the work.
- 修改和变更作品。
- Distribute the code.
- 分发代码。
- Sublicense: incorporate the work into something that has a more restrictive license.
- 再许可：将本作品整合到具有更严格许可证的项目中。
Collapsible card
可折叠卡片
Click the minus icon to collapse; the X to remove.
点击 -图标可  collapse; 点击 X 可删除。
page title + breadcrumb
页面标题 + 面包屑
Light page, light sidebar
亮色页面，亮色侧边栏
Start Navbar links
开始导航栏链接
Light
明亮
Dark
暗黑
Auto
自动
>Home
>首页
>Contact
>联系人
Color Mode Toggler
颜色模式切换
End Navbar links
结束导航栏链接
My Dashboard
我的仪表盘
>MAIN
>首页
Projects
项目
>Active
>活跃
>Archived
>归档
>REPORTS
>报告
Sign out
退出
New orders
新订单
More info
更多信息
Start navbar links
导航栏开始
End navbar links
导航栏结束
Message Start
消息开始
Message End
消息结束
User Avatar
用户头像
Call me whenever you can...
有时间打电话给我……
Hours Ago
小时前
I got your message bro
我收到你的消息了，兄弟
Messages Dropdown Menu
消息下拉菜单
The subject goes here
主题在这里
See All Messages
查看所有消息
15 Notifications
15 条通知
4 new messages
4 条新消息
8 friend requests
8 条好友请求
3 mins
3 分钟前
12 hours
12 小时前
3 new reports
3 个新报告
2 days
2 天前
See All Notifications
查看所有通知
Web Developer
网页开发工程师
Member since Nov. 2023
注册于 2023 年 11 月
The sidebar used in this page to the left provides an example of what your sidebar would look like. Construction of a sidebar:
下方代码中展示了你的侧边栏大致的样子。侧边栏的构建方式如下：
Nav Header
导航栏标题
Level 2 (Badge)
2 级（徽章）
Level 1 Active
1 级活跃
Treeview Menu Open
Treeview 菜单打开
Level 2 Active
2 级活跃
Alternate Logo
备用 Logo
You can use two logo images instead of logo with text, you only need to change the markup to this:
你可以使用两张 Logo 图片来替代带文字的 Logo，只需将 HTML 结构修改为如下所示：
Logo Small
小 Logo
Logo Large
大 Logo
Card collapse / remove / maximize
卡片折叠 / 移除 / 最大化
# Example
# 示例
IE 11 forks available
提供 IE 11 兼容版本
Modern evergreen browsers only
仅支持现代浏览器
Manual `.dark-mode` class
手动设置 `.dark-mode` 类
Edit this file to start building your dashboard.
编辑此文件以开始构建你的仪表盘。
Hello,
你好，
>Reports
>报告
>Welcome
>欢迎
Plus Bootstrap and dependency CSS
添加 Bootstrap 和依赖 CSS
nav items
导航项目
same markup as LTR
与 LTR 相同的标记
LTR app shell
LTR 应用外壳
this block only is RTL
此区块仅支持从右到左（RTL）
>Customer
>客户
>Total
>合计
>Status
>状态
>Actions
>操作
Calls `document.documentElement.requestFullscreen()`. The icon `.d-none` toggle and `maximized.lte.fullscreen` event are driven by the resulting `fullscreenchange` event, so a denied request leaves the UI untouched. |
调用 `document.documentElement.requestFullscreen()`。图标 `.d-none` 的切换和 `maximized.lte.fullscreen` 事件均由随后触发的 `fullscreenchange` 事件驱动，因此如果全屏请求被拒绝，界面将保持原样不变。|
Calls `document.exitFullscreen()`. The icon `.d-none` toggle and `minimized.lte.fullscreen` event are driven by the resulting `fullscreenchange` event. |
调用 `document.exitFullscreen()`。图标 `.d-none` 的切换和 `minimized.lte.fullscreen` 事件均由随后触发的 `fullscreenchange` 事件驱动。|
- Icon state and events are synced from the native `fullscreenchange` event, so they stay correct when the user exits fullscreen with <kbd>ESC</kbd> or <kbd>F11</kbd>, and a request denied by the browser leaves the icons unchanged.
- 图标状态和事件由原生 `fullscreenchange` 事件同步驱动，因此当用户按 <kbd>ESC</kbd> 或 <kbd>F11</kbd> 退出全屏时，状态依然正确；若全屏请求被浏览器拒绝，图标则保持不变。